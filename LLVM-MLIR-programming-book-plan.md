# LLVM, Clang & MLIR — The Expert Programming Reference

### *With Compiler Theory, Type Theory, Polyhedral Theory, and Verified Compilation*
### *Targeting LLVM 22.x, MLIR (in-tree), Clang 22, OpenXLA, and ClangIR (April 2026)*

The complete expert reference for the LLVM compiler ecosystem. ~2200 pages across 176 chapters and 8 appendices in 25 parts. covers, in addition to the LLVM/MLIR/XLA/Clang implementation: the theoretical foundations of compilation (Dragon Book / Cooper-Torczon material), modern type theory (Pierce's TAPL / Harper's PFPL material), the derivation of the polyhedral model from Presburger arithmetic and integer programming, and the formal-verification approach to compilation pioneered by CompCert, Vellvm, and Alive2.

**Pin version:** LLVM 22.1.x. Practical chapters verified against the LLVM 22 release branch. Theoretical chapters verified against the canonical literature (cited per-chapter at draft time).

---

## Scope

**A note on size and density.** ~2200 pages at typical technical-book density. Theoretical chapters are denser per page — the Pluto algorithm derivation, the Hindley-Milner correctness proof, and the CompCert pass-by-pass semantic preservation argument each have higher information density than, say, the X86 backend chapter. Theoretical chapters average ~20 pages each rather than ~12. See the volume-split section.

**A note on verification.** It is CRITICAL that Non-theoretical chapters are verified against LLVM 22 code - **THE CODE MUST BE ACCURATE!!!**. Theoretical chapters cannot be verified against LLVM 22 code — they're verified against the foundational literature. Per-chapter drafting will require checking against:
- **Compiler Theory:** Aho/Lam/Sethi/Ullman (Dragon Book 2/e), Cooper-Torczon (Engineering a Compiler 3/e), Appel (Modern Compiler Implementation), Muchnick (Advanced Compiler Design and Implementation)
- **Type Theory:** Pierce (TAPL, ATTAPL), Harper (PFPL), Mitchell (Foundations for Programming Languages), Reynolds (Theories of Programming Languages)
- **Polyhedral Theory:** Bondhugula's PhD thesis, Feautrier's scheduling papers, Verdoolaege's ISL papers, the Polyhedral Compilation textbook by Grosser et al.
- **Verified Compilation:** Leroy's CompCert papers, Zhao/Zdancewic/Nagarakatte papers on Vellvm, Lopes/Lee/Hur papers on Alive2
- **Mathematical Foundations and Verified Systems (Part XXVII):** Lean 4 reference manual and LCNF compiler docs; Coq/Rocq reference manual; Nipkow/Paulson/Wenzel (Isabelle/HOL: A Proof Assistant for Higher-Order Logic); Church (1936) / Gödel (1931) primary sources; Enderton (A Mathematical Introduction to Logic); Watson/Woodruff/Woodruff (CHERI ISA specification); Watson et al. (CHERI: A Hybrid Capability-System Architecture); Klein et al. (seL4: Formal Verification of an OS Kernel, SOSP 2009); Mattern (The Formal Semantics of seL4, l4v project docs); Tarjan (A Unified Approach to Path Problems, 1981); Cox/Little/O'Shea (Ideals, Varieties, and Algorithms); Mac Lane (Categories for the Working Mathematician, 2e); Awodey (Category Theory, 2e); Riehl (Category Theory in Context, free online); Winskel (Formal Semantics of Programming Languages, MIT Press 1993); Milner (Communication and Concurrency); Hoare (Communicating Sequential Processes)
- **Language Ecosystems and Engineering Practice (Part XXVIII):** Eldridge et al. (CIRCT: MLIR for Hardware Design, ICCAD 2021); FIRRTL language specification; Calyx language specification (ASPLOS 2021); QIR Alliance specification; Cross et al. (OpenQASM 3, ACM TOPLAS 2022); Coecke/Kissinger (Picturing Quantum Processes, CUP 2017); Swift SIL Reference (docs.swift.org); Jordan Rose "Ownership Is Membership" (LLVM Dev Meeting 2018); Bezanson et al. (Julia: A Fresh Approach to Numerical Computing, SIAM Review 2017); Jeff Bezanson PhD thesis (MIT 2015); Zig language reference (ziglang.org); Andrew Kelley LLVM Dev Meeting 2022; DO-178C standard (RTCA SC-205); ISO 26262:2018; IEC 61508:2010; Ferrocene Language Specification (ferrocene.dev); Itanium C++ ABI specification; bindgen user guide; UniFFI documentation

---

## Part I — Foundations *(~85 pages, 5 ch)*

| # | Chapter | Topics |
|---|---------|--------|
| 1 | The LLVM Project | History; the monorepo; full subproject map; release cadence; the umbrella model; what LLVM is *not*; downstream forks landscape |
| 2 | Building LLVM from Source | CMake; Ninja; `LLVM_ENABLE_PROJECTS` vs `LLVM_ENABLE_RUNTIMES`; bootstrap and 2/3-stage builds; ccache/sccache/distcc; cross-compilation; `llvm-cas`-backed builds |
| 3 | The Compilation Pipeline | The full pipeline diagram; where each tool sits; reading the cc1 invocation; pipeline diagrams for all major flows |
| 4 | The LLVM C++ API | RTTI; `LLVMContext`; ADT library; `Support` utilities; `Error` and `Expected`; `cl::opt`; visitor and iterator patterns |
| 5 | LLVM as a Library | `llvm-config`; CMake's `find_package(LLVM)`; component model; the C API; out-of-tree pass plugin and backend skeleton |

## Part II — Compiler Theory and Foundations *(~120 pages, 6 ch)*  

| # | Chapter | Topics |
|---|---------|--------|
| 6 | Lexical Analysis | Regular languages and regular expressions as a recognition formalism; Thompson's NFA construction; subset construction (NFA → DFA); Hopcroft and Brzozowski DFA minimization; lexer generators (`lex`, `flex`, `re2c`, `ragel`); the case for hand-written lexers (Clang, GCC); incremental and lazy lexing for IDEs |
| 7 | Parsing Theory | Context-free grammars; ambiguity and disambiguation; top-down: predictive parsing, LL(k), LL(*), recursive descent; bottom-up: LR(0), SLR(1), LALR(1), LR(1), GLR; Earley parsing; CYK; operator-precedence parsing; Pratt parsing; PEG and packrat; parser combinators; error recovery (panic mode, phrase-level, error productions); incremental parsing for IDEs; the Clang choice of hand-written recursive descent |
| 8 | Semantic Analysis Foundations | Symbol tables (chained, hashed, persistent); scoping models (lexical, dynamic, hierarchical); attribute grammars (synthesised vs inherited); syntax-directed translation schemes; the type-checking judgment as a tree walk; name resolution algorithms; the visitor pattern as compiled attribute grammar |
| 9 | Intermediate Representations and SSA Construction | Why an IR; AST vs three-address code vs CPS vs ANF; the SSA invariant restated formally; the Cytron-Ferrante-Rosen-Wegman-Zadeck dominance-frontier algorithm with derivation; pruned vs minimal vs semi-pruned SSA; out-of-SSA conversion (the lost-copy and swap problems; Briggs-Cooper-Harvey-Reeves and Sreedhar's algorithm); CPS↔SSA equivalence |
| 10 | Dataflow Analysis: The Lattice Framework | Partial orders, lattices, complete lattices; ascending and descending chain conditions; Tarski's fixed-point theorem; Kleene iteration and termination; monotone vs distributive frameworks; transfer functions; the MOP–MFP relationship and when they coincide; widening/narrowing for abstract interpretation; classical analyses derived: liveness, available expressions, reaching definitions, very-busy expressions, constant propagation; interprocedural extensions: call strings, k-CFA, m-CFA; context-sensitivity and the precision-cost tradeoff |
| 11 | Classical Optimization Theory | Local vs global vs interprocedural; common subexpression elimination via value numbering; loop-invariant code motion; strength reduction and induction-variable elimination; partial redundancy elimination (Morel-Renvoise, lazy code motion); register allocation theory: graph coloring (Chaitin), Chaitin-Briggs, iterated register coalescing (George-Appel), linear scan (Poletto-Sarkar), PBQP, SSA-based RA; instruction scheduling: list scheduling, trace scheduling, superblock and hyperblock scheduling, software pipelining (modulo scheduling); garbage collection theory: mark-sweep, copying (Cheney), generational, incremental, concurrent, region-based; mapping back to LLVM's actual implementations |

## Part III — Type Theory *(~80 pages, 4 ch)*  

| # | Chapter | Topics |
|---|---------|--------|
| 12 | Lambda Calculus and Simple Types | Untyped λ-calculus: syntax, α-equivalence, β-reduction; Church-Rosser confluence; weak vs strong normalization; simply typed λ-calculus (STLC); typing judgments and derivation trees; type safety theorems: progress and preservation; the Curry-Howard correspondence (STLC ↔ propositional intuitionistic logic); the connection between proofs and programs |
| 13 | Polymorphism and Type Inference | Parametric polymorphism; System F (second-order λ-calculus); existentials and abstract data types; the rank hierarchy; Hindley-Milner type system; Algorithm W with derivation; Damas-Milner; let-polymorphism and value restriction; principal types; bidirectional type checking; row polymorphism; type classes (Wadler-Blott) and dictionary translation; ad-hoc vs parametric polymorphism contrasted |
| 14 | Advanced Type Systems | Dependent types: Π and Σ types; Martin-Löf type theory; the Calculus of Constructions and Coq's CIC; Lean 4's dependent type theory; subtyping: width, depth, structural; bounded quantification (System F<:); linear logic and linear types; affine types and Rust's borrow checker formally; uniqueness types; region types and Tofte-Talpin; effect systems (Marino-Millstein, Koka); refinement types and Liquid types; gradual typing (Siek-Taha); session types; dependent types for systems programming (ATS, F*) |
| 15 | Type Theory in Practice: From Theory to LLVM and MLIR | LLVM IR's type system as a (mostly) nominal-structural hybrid without polymorphism; why generics monomorphize before reaching LLVM; MLIR types and the more theory-aligned design (parameterised types, type interfaces); how Clang implements C/C++ types (canonical types, sugar, dependent types in templates); Rust's borrow checker as a substructural type system over MIR; how ML compilers (HM-typed) elaborate to LLVM; refinement-style verification on LLVM IR via Alive2; the C++ `constexpr`/`consteval` calculus and its relationship to total functional programming |

## Part IV — LLVM IR *(~225 pages, 12 ch)*

| # | Chapter | Topics |
|---|---------|--------|
| 16 | IR Structure | Modules, functions, basic blocks, instructions; textual `.ll` vs bitcode `.bc`; printing/parsing; `llvm-dis`/`llvm-as`; module-level inline asm; module flags |
| 17 | The Type System | Primitives; arbitrary-width integers; FP types incl. `bfloat`/`fp128`/`x86_fp80`/`ppc_fp128`; vectors fixed and scalable; arrays and structs; opaque pointers (`ptr`); target extension types; `DataLayout` |
| 18 | Constants, Globals, and Linkage | `ConstantExpr`; `ConstantData`; globals; thread-locals (all four TLS models); aliases and ifuncs; full linkage taxonomy; `dso_local`; `dllimport`/`dllexport`; comdats |
| 19 | Instructions I — Arithmetic and Memory | `nsw`/`nuw`/`exact`/`disjoint`; FP fast-math flags; `getelementptr` index-calculation rules; load/store with align/volatile/atomic; `alloca`; `freeze`; `ptrtoint`/`inttoptr` and provenance |
| 20 | Instructions II — Control Flow and Aggregates | `br`/`switch`/`indirectbr`/`callbr`; `call`/`invoke`; `phi`; `select`; aggregate ops; vector ops; EH instructions (`landingpad`, `cleanuppad`, `catchpad`, `catchswitch`); `unreachable`/`ret`/`resume` |
| 21 | SSA, Dominance, and Loops | Phi placement; dominator/post-dominator trees; loops, headers, latches, exits; natural loops; LoopInfo; LoopNest; cycle terminology; iterated dominance frontier (cross-ref Ch 9) |
| 22 | Metadata and Debug Info | `!metadata`; `MD_tbaa`/`MD_range`/`MD_loop`/`MD_prof`/`MD_unpredictable`/`MD_alias_scope`; DWARF metadata family; the new debug-record format (RemoveDIs); pseudo-probe metadata |
| 23 | Attributes, Calling Conventions, and the ABI | Full attribute reference; attribute groups; calling conventions catalogue; the ABI lowering boundary; sret/byval/byref nuances |
| 24 | Intrinsics | Target-independent intrinsics; vector predication (`llvm.vp.*`); constrained FP and the FP environment; per-target intrinsic catalogues |
| 25 | Inline Assembly | Constraint syntax; register classes; `volatile`/`inteldialect`; memory clobbers; multiple alternatives; how Clang lowers `__asm__`; MS-style inline asm |
| 26 | Exception Handling | Itanium model; SEH (Windows); Wasm EH; `llvm.eh.*`; `__cxa_throw` to unwinder path; cross-language EH; setjmp/longjmp EH |
| 27 | Coroutines and Atomics | `llvm.coro.*` lowering; the four coro passes; switched-resume vs returned-continuation; atomic orderings; `cmpxchg` weak/strong; `atomicrmw`; sync scopes; the C/C++ memory model lowering |

## Part V — Clang Internals: Frontend Pipeline *(~210 pages, 11 ch)*

| # | Chapter | Topics |
|---|---------|--------|
| 28 | The Clang Driver | Driver vs `-cc1`; `ToolChain`; argument translation; multi-stage jobs; offload bundling at the driver level |
| 29 | SourceManager, FileEntry, SourceLocation | Compressed `SourceLocation`; FileID and offset encoding; macro-expansion locations; spelling vs expansion; line tables; `FileManager` and VFS |
| 30 | The Diagnostic Engine | `DiagnosticsEngine`; severity mapping; group hierarchies; `-W` plumbing; `clang-tblgen` for diagnostics; FixIt hints; SARIF; serialized diagnostics |
| 31 | The Lexer and Preprocessor | Token model; lexer state machine; macro expansion algorithm (object-like vs function-like, hygiene, rescanning); `__has_*` builtins; `#pragma`; conditional compilation; header search; the preprocessor as a library |
| 32 | The Parser | Recursive-descent organization; ambiguity resolution; error recovery; tentative parsing; Parser ↔ Sema action interface; specific challenges (most-vexing-parse, template angle brackets, requires-clauses); cross-ref Ch 7 for theoretical background |
| 33 | Sema I — Names, Lookups, and Conversions | Name lookup (unqualified, qualified, two-phase, ADL); overload resolution; implicit conversion sequences; reference binding; access control; ambiguity and visibility |
| 34 | Sema II — Templates, Concepts, and Constraints | Template parameter deduction; partial ordering; SFINAE; the `InstantiatingTemplate` stack; class/function/variable/alias template instantiation; variadic templates; concepts and requires-clauses; constraint normalisation and subsumption (cross-ref Ch 13) |
| 35 | The Constant Evaluator | `ExprConstant.cpp`; the `Evaluate` method family; integer constant expressions vs general constant expressions; `constexpr`/`consteval`/`constinit`; pointer/reference constant evaluation; `bit_cast`; immediate-invocation contexts |
| 36 | The Clang AST in Depth | `ASTContext`; `Decl` hierarchy; `Stmt`/`Expr` hierarchies; `Type` hierarchy (canonical types, sugar, dependent); `DeclContext` and lookup; `ASTReader`/`ASTWriter` |
| 37 | C++ Modules Implementation | Clang Modules (`-fmodules`); C++20 Modules; BMI files; module maps; the prebuilt-module-cache; module compilation database |
| 38 | Code Completion and clangd Foundations | `CodeCompleteConsumer`; semantic vs lexical completion; incremental AST building; index-while-building; background indexing; dynamic vs static index |

## Part VI — Clang Internals: Codegen and ABI *(~120 pages, 6 ch)*

| # | Chapter | Topics |
|---|---------|--------|
| 39 | CodeGenModule and CodeGenFunction | Module-level state; lazy emission; deferred decls; emission of globals; the function emission flow; debug-info and EH state per function |
| 40 | Lowering Statements and Expressions | `CGStmt`; `CGExpr` for lvalues and rvalues; aggregate vs scalar emission; complex-number emission; `CGExprConstant`; `CGExprAgg` |
| 41 | Calls, the ABI Boundary, and Builtins | `CGCall`; `ABIInfo` and per-target `TargetCodeGenInfo`; argument classification (SysV, Win64, AAPCS, AAPCS64, RISC-V); split aggregates; sret/byval/byref legalization; the builtin lowering tables |
| 42 | C++ ABI Lowering: Itanium | `CGCXXABI`/`ItaniumCXXABI`; `VTableBuilder`; construction vtables and VTT for virtual inheritance; thunks; thread-safe statics; RTTI emission; the Itanium name mangler |
| 43 | C++ ABI Lowering: Microsoft | `MicrosoftCXXABI`; vftables and vbtables; MS this-pointer adjustment; MS RTTI; MS thread-safe statics; the Microsoft name mangler; SEH-driven cleanup |
| 44 | Coroutine Lowering in Clang | `CoroutineStmt`; lowering to `llvm.coro.*`; promise-type integration; symmetric transfer |

## Part VII — Clang as a Multi-Language Compiler *(~140 pages, 7 ch)*

| # | Chapter | Topics |
|---|---------|--------|
| 45 | The Static Analyzer | Symbolic-execution engine; ExplodedGraph; checkers; path-sensitive vs flow-sensitive; cross-translation-unit analysis; writing custom checkers |
| 46 | libtooling and AST Matchers | `libtooling`; `CompilationDatabase`; `RecursiveASTVisitor` vs `DynTypedVisitor`; the AST-matchers DSL; rewriter-based source transformations |
| 47 | clangd, clang-tidy, clang-format, clang-refactor | clangd architecture and LSP feature mapping; check authoring; the `clang-format` engine; `clang-include-cleaner`; `clang-doc` |
| 48 | Clang as a CUDA Compiler | Single-source compilation; host vs device passes; offload bundler; PTX emission via NVPTX; libdevice |
| 49 | Clang as a HIP Compiler | HIP language vs CUDA; AMDGPU code object format; ROCm runtime integration; rocm-device-libs |
| 50 | Clang as SYCL, OpenCL, and OpenMP-Offload | SYCL 2020 single-source; the integration header; OpenCL frontend; OpenMP target offload |
| 51 | Clang as an HLSL Compiler | HLSL frontend; resource bindings; DXIL backend; SPIR-V output for Vulkan; root signatures; comparison with DXC |

## Part VIII — ClangIR (CIR) *(~50 pages, 3 ch)*

| # | Chapter | Topics |
|---|---------|--------|
| 52 | ClangIR Architecture | The CIR MLIR dialect; CIR's pipeline position; `-fclangir`/`-emit-cir`; upstreaming status |
| 53 | CIR Generation from AST | `CIRGenModule`/`CIRGenFunction`; AST-to-CIR translation; CIR types and ops; back-references to AST |
| 54 | CIR Lowering and Analysis | Direct-to-LLVM and through-MLIR-dialects lowering; the lifetime checker; idiom recognition |

## Part IX — Frontend Authoring (Building Your Own) *(~80 pages, 4 ch)*

| # | Chapter | Topics |
|---|---------|--------|
| 55 | Building a Frontend | Lexer/parser; AST design; the Kaleidoscope reference walkthrough; building a small expression language |
| 56 | Lowering AST to IR | `IRBuilder<>`; emitting expressions; visitor patterns; emission of control flow with phi; alloca-then-mem2reg; debug info alongside codegen |
| 57 | Lowering High-Level Constructs | Aggregates; arrays and bounds; vtables; closures; tagged unions; coroutines; string literals; RTTI emission |
| 58 | Language Runtime Concerns | GC strategies and statepoints; TLS lowering models; atomics and the C/C++ memory model; stack maps; signal-safety |

## Part X — Analysis and the Middle-End *(~210 pages, 11 ch)*

| # | Chapter | Topics |
|---|---------|--------|
| 59 | The New Pass Manager | `PassManager`, `AnalysisManager`; analysis vs transform passes; pass instrumentation; pipeline parsing; `default<O3>` |
| 60 | Writing a Pass | Out-of-tree pass plugin; `FunctionPass`/`LoopPass`/`ModulePass`/`CGSCCPass`; preserving analyses; pass registration |
| 61 | Foundational Analyses | DominatorTree; PostDominatorTree; LoopInfo; AliasAnalysis variants; MemorySSA; ScalarEvolution; DemandedBits; LazyValueInfo; BranchProbabilityInfo; BlockFrequencyInfo (cross-ref Ch 10) |
| 62 | Scalar Optimizations | DCE/ADCE/BDCE; GVN/NewGVN; SCCP/IPSCCP; InstCombine and InstSimplify; SimplifyCFG; Reassociate; EarlyCSE; CorrelatedValuePropagation; JumpThreading; TailCallElim |
| 63 | Loop Optimizations | LICM; LoopRotate; LoopUnroll; IndVarSimplify; LoopUnswitch; LoopIdiom; LoopDeletion; LoopFusion; LoopDistribute; LoopInterchange; LoopFlatten; LoopVersioning |
| 64 | Vectorization Deep Dive | The Loop Vectorizer; VPlan in detail; the SLP Vectorizer; cost models; scalable vectorization; predication and tail folding |
| 65 | Inter-Procedural Optimizations | Inliner; GlobalOpt; ArgPromotion; FunctionAttrs inference; DeadArgElim; MergeFunctions; PartialInlining; OpenMPOpt; AttributorPass |
| 66 | The ML Inliner and ML Regalloc | The RL-trained inliner; offline training pipeline; ML eviction policy in regalloc; the TFLite runtime dependency |
| 67 | Profile-Guided Optimization | Instrumentation-based PGO; SampleProfile/AutoFDO; pseudo-probes; CSSPGO; `llvm-profdata`; ThinLTO + PGO; PGO + BOLT |
| 68 | Hardening and Mitigations | CFI variants; SafeStack; ShadowCallStack; Speculative Load Hardening; Stack Clash; Stack Protector; PAuth/BTI; Intel CET/IBT; FORTIFY_SOURCE; Hot/Cold splitting |
| 69 | Whole-Program Devirtualization | The WPD pass; type-test/type-checked-load; LowerTypeTests; CFI/WPD shared infrastructure; integration with LTO/ThinLTO |

## Part XI — Polyhedral Theory *(~80 pages, 4 ch)*  

| # | Chapter | Topics |
|---|---------|--------|
| 70 | Foundations: Polyhedra and Integer Programming | Convex polyhedra; H-representation and V-representation; faces, facets, vertices; lattices and integer points; Hermite and Smith normal forms; linear programming and the simplex method; integer linear programming and branch-and-bound; Presburger arithmetic; quantifier elimination; the Omega test; ISL's algorithmic foundations |
| 71 | The Polyhedral Model | Iteration spaces as polyhedra; access functions and image computation; the Static Control Part (SCoP) and its restrictions; dependence relations (flow, anti, output, input); dependence polyhedra; exact dependence vs may-dependence; uniformization and parametric dependences; the affine schedule space |
| 72 | Scheduling Algorithms | Feautrier's multidimensional scheduling; Lim-Lam scheduling for affine partitioning; the Pluto algorithm: tiling-aware scheduling, the cost function derivation, permutability and tilability; affine transformations realised as schedules: skewing, fusion, fission, distribution, interchange, reversal; iterative compilation and autotuning; recent extensions (PoCC, isl scheduler) |
| 73 | Code Generation from Polyhedral Schedules | The CLooG algorithm; isl's AST generator; loop bound generation; loop fusion and distribution legality; tiling: hyperrectangular, parallelogram, time-skewed, diamond; generation of OpenMP parallel pragmas; SIMD-aware codegen; mapping to GPU code (the Polly-ACC and PPCG approaches); unroll-and-jam at the polyhedral level |

## Part XII — Polly *(~50 pages, 3 ch)*

| # | Chapter | Topics |
|---|---------|--------|
| 74 | Polly Architecture | ScopDetection and ScopInfo; the SCoP representation in Polly; integration with the LLVM pass pipeline; ISL inside Polly; comparison to Pluto's reference implementation |
| 75 | Polly Transformations | How Polly maps the theory of Part XI to LLVM IR; tile-size selection; loop fusion in practice; the JSON schedule format and exporting/re-importing schedules |
| 76 | Polly in Practice | When Polly helps and when it doesn't; auto-tuning; OpenMP parallel-loop generation; the GPU codegen path (experimental); current maintenance status |

## Part XIII — Link-Time and Whole-Program *(~80 pages, 4 ch)*

| # | Chapter | Topics |
|---|---------|--------|
| 77 | LTO and ThinLTO | Monolithic LTO; ThinLTO summary index; cross-module inlining/type propagation; LLD/gold plugin integration; Distributed ThinLTO |
| 78 | The LLVM Linker (LLD) | LLD architecture; ELF/COFF/MachO/Wasm drivers; symbol resolution; relocations and relaxations; linker scripts; partial linking; ICF |
| 79 | Linker Internals: GOT, PLT, TLS | The Global Offset Table; the Procedure Linkage Table; lazy binding; TLS dispatch (TLSDESC, IE, LE, GD, LD); IFUNC resolution; eh_frame layout; `.ctors`/`.dtors` and `.init_array`; **linker relaxation in depth**: RISC-V `R_RISCV_CALL`→`R_RISCV_JAL` and GP-relative relaxation (`--relax-gp`); AArch64 `ADRP+ADD`→`ADR` relaxation; x86-64 `GOTPCRELX`/`REX_GOTPCRELX` GOT-elimination; TLS relaxation chains (GD→IE→LE for each target); how relaxation interacts with section layout ordering, symbol resolution, and `--gc-sections`; the relaxation fixup pass in LLD |
| 80 | llvm-cas and Content-Addressable Builds | The CAS object model; integration with Clang for module reuse; CASFS for hermetic builds; the CAS Plugin API; **reproducible builds and toolchain supply-chain security**: the bootstrappable-builds movement; Ken Thompson's "Reflections on Trusting Trust" — the trusting-trust attack; how llvm-cas provides content-addressable reproducibility guarantees; binary-reproducibility verification (`diffoscope`); SBOM generation for toolchain releases; signed LLVM release verification; Guix/Nix hermetic compilation compared; the bootstrap path from a seed binary |

## Part XIV — The Backend *(~280 pages, 14 ch)*

| # | Chapter | Topics |
|---|---------|--------|
| 81 | Backend Architecture | The codegen pipeline; LLT vs MVT vs EVT; SDAG vs GISel coexistence |
| 82 | TableGen Deep Dive | `.td` syntax; multiclass, foreach, defm, defvar, !-operators; record graph; backends (`-gen-instr-info`, `-gen-asm-matcher`, `-gen-dag-isel`, `-gen-global-isel`, `-gen-register-info`, `-gen-callingconv`, `-gen-subtarget`) |
| 83 | The Target Description | `TargetMachine`, `Subtarget`, `TargetLowering`; register classes; instruction info; calling convention description; predicates and features; `SchedMachineModel`, itineraries |
| 84 | SelectionDAG: Building and Legalizing | SelectionDAGBuilder; ISD opcodes; type and operation legalization; vector splitting and widening |
| 85 | SelectionDAG: Combining and Selecting | DAGCombiner; target DAG combines; pattern-based instruction selection; custom selection in C++; isel debugging |
| 86 | GlobalISel | IRTranslator → Legalizer → RegBankSelect → InstructionSelect; LLT type system; the GlobalISel combiner DSL; falling back to SDAG |
| 87 | Inline Assembly Lowering | Backend-side constraint handling; tied operands; clobbers and `INLINEASM`; integrated assembler interaction (cross-ref Ch 25) |
| 88 | The Machine IR | `MachineFunction`, `MachineBasicBlock`, `MachineInstr`, `MachineOperand`; properties; MIR text format; `PseudoSourceValue` and `MachineMemOperand` |
| 89 | Pre-RegAlloc Passes | TwoAddressInstructionPass; PHI elimination; live interval analysis; coalescing; pre-RA scheduling (machine combiner, `MachineScheduler`) |
| 90 | Register Allocation | The greedy allocator deep dive; live range splitting; spill weights; eviction; reg-bank-aware allocation; PBQP, basic, fast allocators; spill placement (cross-ref Ch 11) |
| 91 | The Machine Pipeliner | Software pipelining; modulo scheduling; Swing modulo scheduling; VLIW targets; current target uptake (Hexagon, PowerPC) |
| 92 | The Machine Outliner | The MachineOutliner pass; suffix-tree-based candidate detection; cost models; per-target hooks; size-vs-perf tradeoffs |
| 93 | Post-RegAlloc and Pre-Emit | Prologue/epilogue insertion; stack frame layout; branch folding; tail duplication; if-conversion; post-RA scheduling; bundling; MachineFunctionSplit; CFGuard |
| 94 | The MC Layer and MIR Test Infrastructure | `MCStreamer`, `MCAssembler`, `MCObjectWriter`; `MCInst` vs `MachineInstr`; assembly printing; integrated assembler; relocations and fixups; `MCDisassembler`; `.mir` format |

## Part XV — Targets *(~250 pages, 13 ch)*

| # | Chapter | Topics |
|---|---------|--------|
| 95 | The X86 Backend | GPR/XMM/YMM/ZMM/k registers; calling conventions; SSE/AVX/AVX-512; APX (EGPR, NDD); CET; complex addressing; `__attribute__((target))` and multiversioning; MS x64 ABI specifics |
| 96 | The AArch64 Backend | AAPCS64; NEON; SVE/SVE2; SME and streaming mode; PAuth; BTI; LSE atomics; Apple-specific variants; outlining |
| 97 | The 32-bit ARM Backend | A32/T32 ISA mix; Thumb interworking; AAPCS; VFP/NEON; Cortex-M; `arm-none-eabi` |
| 98 | The RISC-V Backend Architecture | Base ISA + scalar extensions; calling conventions (LP64, ILP32 variants); relaxation; subtarget features; profiles (RVA22, RVA23) |
| 99 | The RISC-V Vector Extension (RVV) | The V extension in detail; vtype/vl/vlenb; LMUL and SEW; tail-/mask-agnostic policies; tuple types; LLVM's scalable-vector type modelling; the VLS optimizer; Loop Vectorizer integration |
| 100 | RISC-V Bit-Manip, Crypto, and Custom Extensions | Zb*, Zk*, Zc*, Zfh/Zfa, Zicfilp/Zicfiss; vendor extensions (XTHead, XSiFive); how to add a custom extension upstream |
| 101 | PowerPC, SystemZ, MIPS, SPARC, LoongArch | Server-class targets compared |
| 102 | NVPTX and the CUDA Path | PTX as a target; address spaces; texture/surface; libdevice; `nvvm` intrinsics; tensor-core MMA intrinsics |
| 103 | AMDGPU and the ROCm Path | GCN/RDNA/CDNA wave model; SGPR/VGPR; LDS; SI scheduling; AMDGPU code object v5; rocm-device-libs; HSA queue ABI |
| 104 | The SPIR-V Backend | SPIR-V as a target; logical vs physical addressing; capability and extension model; entry-point handling; comparison with the Khronos translator |
| 105 | DXIL and DirectX Shader Compilation | The DXIL backend; resource binding lowering; root signature; comparison with DXC; signing and validation |
| 106 | WebAssembly and BPF | Wasm: SSA-on-stack mismatch, multi-value, SIMD128, reference types, GC proposal, exceptions, threads. eBPF: verifier-driven constraints, BTF, CO-RE relocations; **WASI (WebAssembly System Interface)**: the WASI SDK and `wasi-libc`; WASI preview 1 vs preview 2 (component-model-based); `wasm32-wasip1` and `wasm32-wasip2` Clang target triples; `clang --target=wasm32-wasi` and sysroot layout; WASI reactor vs command model; WASI socket extensions; **WasmGC proposal**: typed references and heap types (`funcref`, `externref`, `anyref`, `eqref`, struct/array heap types); the Clang/LLVM WasmGC lowering path; comparison with Emscripten's JavaScript-emulated GC approach |
| 107 | Embedded Targets | AVR; MSP430; Hexagon (DSP, VLIW); M68k; Xtensa; Lanai; CSky; bare-metal toolchain considerations |

## Part XVI — JIT, Sanitizers, and Diagnostic Tools *(~210 pages, 11 ch)*

| # | Chapter | Topics |
|---|---------|--------|
| 108 | The ORC JIT | Layered architecture; JITDylib; lazy and speculative compilation; remote JIT; LLJIT helper |
| 109 | JITLink | The `LinkGraph`; section/atom-based linking; per-format passes; debugging JIT'd code |
| 110 | User-Space Sanitizers | ASan, TSan, MSan, UBSan, LeakSanitizer; sanitizer common runtime; **DataFlowSanitizer (DFSan)**: taint tracking via shadow memory; `dfsan_label` and label propagation rules; origin tracking mode (`-fsanitize-dataflow-track-origins`); the DFSan ABI: fast8 vs fast16 label schemes; use cases: information-flow security analysis, taint-guided fuzzing, third-party library auditing; integration with ASan; `dfsan_set_label`/`dfsan_get_label`/`dfsan_has_label` API |
| 111 | HWASan and MTE | Hardware-Address Sanitizer; Memory Tagging Extension; KMTE; deployment in production |
| 112 | Production Allocators: Scudo and GWP-ASan | Scudo's hardened-allocator architecture; GWP-ASan sample-based detection; integration with Bionic and Fuchsia |
| 113 | Kernel Sanitizers | KASAN; KMSAN; KCSAN; KFENCE; kernel-side runtime requirements |
| 114 | LibFuzzer and Coverage-Guided Fuzzing | `-fsanitize=fuzzer`; SanitizerCoverage; the libFuzzer driver loop; structured fuzzing; OSS-Fuzz |
| 115 | Source-Based Code Coverage | `-fprofile-instr-generate -fcoverage-mapping`; the coverage mapping format; branch and MC/DC coverage; HTML and lcov export |
| 116 | LLDB Architecture | Process plugins; expression evaluator; DWARF + accelerators; Python scripting bridge; remote debugging; LLDB-DAP |
| 117 | DWARF and Debug Info | DWARF 5; split DWARF; DWP files; CodeView (Windows); how Clang emits debug info; `dsymutil` |
| 118 | BOLT and Post-Link Optimization | BOLT pipeline; perf-based profile collection; basic-block reordering; ICF; PGO + BOLT stacking |

## Part XVII — Runtime Libraries *(~110 pages, 6 ch)*

| # | Chapter | Topics |
|---|---------|--------|
| 119 | compiler-rt Builtins | Polyfills; soft-float; integer division; 128-bit ops; profile and coverage runtimes |
| 120 | libunwind | The unwinding ABI; `_Unwind_*` API; DWARF CFI vs SEH; libunwind on bare metal |
| 121 | libc++ | Architecture; ABI versioning; the inline-namespace trick; modules support; hardening modes; PSTL backends |
| 122 | libc++abi | The Itanium C++ ABI implementation; type info; thread-safe statics; demangling; `__dynamic_cast` |
| 123 | LLVM-libc | Goals and scope; the public-only-headers approach; per-function selection; testing infrastructure |
| 124 | OpenMP and Offload Runtimes | libomp; task scheduling; affinity; `omptarget` and liboffload; the `_OPENMP` macro version matrix |

## Part XVIII — Flang *(~80 pages, 4 ch)*

| # | Chapter | Topics |
|---|---------|--------|
| 125 | Flang Architecture and Driver | Flang vs Classic Flang; parser; semantic analysis; symbol tables; the driver (`flang-new`); module file format |
| 126 | The HLFIR and FIR Dialects | HLFIR semantics; FIR closer-to-LLVM; lowering between them; codegen down to LLVM IR |
| 127 | Flang OpenMP and OpenACC | OpenMP lowering via the OpenMP MLIR dialect; OpenACC pipeline; GPU offload through Flang |
| 128 | Flang Codegen and Runtime | The Fortran runtime; array intrinsics; I/O runtime; descriptor-based dispatch; do-concurrent lowering |

## Part XIX — MLIR Foundations *(~150 pages, 8 ch)*

| # | Chapter | Topics |
|---|---------|--------|
| 129 | MLIR Philosophy | Why "multi-level"; dialects as first-class units; progressive lowering; comparison with LLVM IR, Swift SIL, Rust MIR, GIMPLE |
| 130 | MLIR IR Structure | Operations; results, operands, attributes, regions, successors; SSA values; symbol tables; nested regions and isolation |
| 131 | The Type and Attribute Systems | Built-in types; defining custom types/attributes; storage uniquing; type/attribute interfaces (cross-ref Part III) |
| 132 | Defining Dialects with ODS | TableGen for MLIR; `Op<>`; traits and interfaces; verifiers; assembly-format DSL; constraints |
| 133 | Op Interfaces and Traits | `OpInterface`; common interfaces; SymbolOpInterface; **external models and retroactive interface attachment**: `registerExternalModels` — attaching interfaces to ops defined in other dialects without modifying their `.td` files; the `DLTIDialect` and `TargetSystemSpecAttr` as canonical examples; use in downstream projects that don't own the dialect; `mlir::registerBufferizableOpInterfaceExternalModels` as a real-world pattern; trade-offs vs modifying the dialect directly |
| 134 | The MLIR C++ API | `OpBuilder`, `RewriterBase`; `walk`; PatternRewriter API; locations and diagnostics |
| 135 | PDL and PDLL | Pattern Description Language; PDLL frontend; declarative rewrite specification; PDL vs DRR vs C++ patterns |
| 136 | MLIR Bytecode and Serialization | The bytecode format; round-tripping; versioning; cross-version compatibility |

## Part XX — In-Tree Dialects *(~190 pages, 10 ch)*

| # | Chapter | Topics |
|---|---------|--------|
| 137 | Core Dialects | `builtin`, `func`, `arith`, `math`, `complex`, `cf`, `scf`, `ub`; canonicalizations and folders |
| 138 | Memory Dialects | `memref` in detail; `bufferization`; one-shot bufferize; the deallocation pass |
| 139 | Tensor and Linalg | `tensor` ops; `linalg` named ops vs `linalg.generic`; indexing maps; iterator types; tiling and fusion; destination-passing style |
| 140 | Affine and SCF | The polyhedral subset (cross-ref Part XI); `affine.for`/`affine.if`; affine maps and sets; loop transformations; **the MLIR Presburger arithmetic library** (`mlir/lib/Analysis/Presburger/`): `IntegerPolyhedron`, `PresburgerSet`, `IntegerRelation`, `PWMAFunction`; the Omega test implementation for integer linear arithmetic; quantifier elimination over integer sets; use in affine dependence analysis (`mlir::checkMemrefAccessDependence`) and loop fusion legality; comparison with ISL — MLIR Presburger is pure C++ with no external library dependency (cross-ref Ch70) |
| 141 | Vector and Sparse | `vector` dialect; `sparse_tensor` encoding and codegen |
| 142 | GPU Dialect Family | `gpu`, `nvgpu`, `nvvm`, `amdgpu`, `rocdl`; the host-device split |
| 143 | SPIR-V Dialect | Modeling; capability/extension model; entry-point ops; lowering from `gpu`/`vector`/`memref`; serialization |
| 144 | Hardware Vector Dialects | `amx`, `arm_sve`, `arm_sme`, `x86vector` |
| 145 | LLVM Dialect | The bottom of the MLIR tower; mapping MLIR types to LLVM types; translation to LLVM IR |
| 146 | Async, OpenMP, OpenACC, DLTI, EmitC | `async`; OpenMP dialect; OpenACC dialect; DLTI; EmitC |

## Part XXI — MLIR Transformations *(~110 pages, 6 ch)*

| # | Chapter | Topics |
|---|---------|--------|
| 147 | Pattern Rewriting | `RewritePattern`, `OpRewritePattern`; `applyPatternsAndFoldGreedily`; canonicalization; folder vs canonicalizer vs rewriter |
| 148 | Dialect Conversion | `ConversionTarget`, `TypeConverter`, `ConversionPattern`; partial vs full conversion; materialization; signature conversion |
| 149 | The Pass Infrastructure | Pass classes; nesting; pipeline grammar; pass instrumentation; multithreaded pass execution |
| 150 | The Transform Dialect | Transform IR as a script; named sequences; pattern interpreters |
| 151 | Bufferization Deep Dive | One-shot bufferize algorithm; `BufferizableOpInterface`; in-place vs out-of-place; the buffer deallocation pipeline |
| 152 | Lowering Pipelines | `linalg → tensor/scf → vector → memref → llvm`; conversion to LLVM IR via `mlir-translate` |

## Part XXII — XLA and the OpenXLA Stack *(~120 pages, 6 ch)*

| # | Chapter | Topics |
|---|---------|--------|
| 153 | XLA Architecture | OpenXLA; XLA's place between frameworks and hardware; the HLO compilation pipeline; relationship to MLIR; buffer assignment |
| 154 | HLO and StableHLO | HLO operation set; layouts and the layout-assignment pass; StableHLO portability layer; backward-compatibility; bytecode serialization; quantization; dynamism |
| 155 | XLA:CPU | LLVM IR emission; runtime calls (Eigen, oneDNN); XLA Runtime and RuntimeIR; AOT compilation |
| 156 | XLA:GPU | Native (LLVM/PTX) emitters vs Triton-based emitters; the priority-fusion cost model; CUDA graph extraction; cuBLAS/cuDNN/NCCL integration |
| 157 | PJRT — The Plugin Runtime Interface | The PJRT C API; framework-agnostic device plugins (CUDA, ROCm, TPU, Metal, Intel GPU); execution model |
| 158 | SPMD, GSPMD, and Auto-Sharding | Sharding annotations; the GSPMD partitioner; auto-sharding inference; collective generation; PyTorch-XLA SPMD |

## Part XXIII — MLIR in Production *(~160 pages, 8 ch)*

| # | Chapter | Topics |
|---|---------|--------|
| 159 | Building a Domain-Specific Compiler | A complete worked example; per-layer testing strategy |
| 160 | MLIR Python Bindings | `mlir-python-bindings`; `Context`, `Module`, `OpView`; building IR from Python |
| 161 | torch-mlir, ONNX-MLIR, and JAX/TF Bridges | torch-mlir; ONNX-MLIR; PyTorch and JAX hand-off through StableHLO |
| 162 | IREE — A Deployment Compiler | Architecture; the dispatch region model; backend targets; the VM and HAL |
| 163 | Triton — A Compiler for GPU Kernels | Triton's MLIR-based stack; the Triton dialect; tile-level abstractions; PyTorch integration |
| 164 | CUDA Tile IR | NVIDIA's MLIR-based dialect for tile-based tensor-core programming; type system for tiles; transformation passes |
| 165 | GPU Compilation Through MLIR | Kernel outlining; serialization to PTX/HSACO/SPIR-V; the host-device split |
| 166 | Mojo, Polygeist, Enzyme-MLIR, and Beyond | Mojo's dialect stack; Polygeist's C/C++ → MLIR; Enzyme-MLIR for autodiff; CIRCT for hardware |

## Part XXIV — Verified Compilation *(~110 pages, 5 ch)*  

| # | Chapter | Topics |
|---|---------|--------|
| 167 | Operational Semantics and Program Logics | Small-step vs big-step semantics; structural operational semantics; reduction-context semantics; Hoare logic with derivation; weakest preconditions; separation logic; concurrent separation logic; Iris; the relationship to the type-safety theorems of Part III |
| 168 | CompCert | The CompCert C subset and its rationale; the pass architecture in detail (Clight → C#minor → Cminor → CminorSel → RTL → LTL → LTLin → Linear → Mach → Asm); semantic preservation as the formal correctness statement; the Coq formalization workflow; the trusted base (parser, assembler, OS); empirical results (Csmith findings); industrial adoption (Airbus, MTU); CompCert's limitations and what it doesn't verify |
| 169 | Vellvm and Formalizing LLVM IR | The Vellvm project; the LLVM IR semantic model formally (small-step + interaction trees); memory models in Vellvm; the undef and poison semantics formally; verified passes (mem2reg as a flagship example); Vellvm vs CompCert: middle-end vs full-pipeline approach; the K-LLVM and Velldoc adjacent work |
| 170 | Alive2 and Translation Validation | Translation validation as a complement to full verification; Alive (peephole verification) → Alive2 (general); Z3-based verification of LLVM optimizations; the IR refinement relation formally; how Alive2 has driven LLVM-IR semantic clarification; the undef/poison/freeze refinement story; integration with LLVM's continuous integration; identified soundness bugs |
| 171 | The Undef/Poison Story Formally | The history: undef → poison → freeze → noundef; the semantic justification; why undef is being phased out; the IR refinement relation; the "twin allocation" model and why it was abandoned; the current status; remaining issues; what `noundef`, `noundef-on-result`, and frozen-poison semantics imply for backends; cross-references to Ch 19 (instruction semantics) and Ch 170 (verification) |

## Part XXV — Operations, Bindings, and Contribution *(~100 pages, 5 ch)*

| # | Chapter | Topics |
|---|---------|--------|
| 172 | Testing in LLVM and MLIR | `lit`; `FileCheck`; `update_test_checks.py`; `gtest`; `ninja check-all`; ABI tests; differential testing (Csmith, yarpgen) |
| 173 | Debugging the Compiler | `--debug-only`; `LLVM_DEBUG`; `-print-after`/`-before`/`-changed`; bisecting passes; reducing test cases (`llvm-reduce`, `bugpoint`, `mlir-reduce`); `llvm-symbolizer`; debug-info validation; **mlir-lsp-server**: the MLIR Language Server Protocol server; dialect registration for LSP (`MlirLspServerSetupFn`); hover/go-to-definition/completion/inlay-hints for `.mlir` files; the `mlir-vscode` VS Code extension; `mlir-pdll-lsp-server` for PDLL files; `mlir-query` for IR pattern exploration at the command line; integrating custom dialects into the LSP server |
| 174 | Performance Engineering | Compile-time profiling; `-ftime-trace`; pass timing reports; quadratic-pass detection; the buildbot fleet |
| 175 | Language Bindings | The C API; the MLIR C API; OCaml; Python (llvmlite, mlir-python-bindings); Rust (Inkwell, llvm-sys); Go; Haskell |
| 176 | Contributing to LLVM | The GitHub workflow; commit-message conventions; pre-merge CI; the RFC process; release branch policy; security policy |

## Part XXVI — Ecosystem and Frontiers *(~108 pages, 7 ch)*

| # | Chapter | Topics |
|---|---------|--------|
| 177 | rustc: Architecture, MIR, and Codegen Backends | rustc architecture: driver → HIR → THIR → MIR → codegen; the Rust compiler dev guide as reference; MIR: `Place`/`Rvalue`/`Terminator`; MIR as Rust's mid-level SSA-like IR; borrow-checker output on MIR; **Miri** — the MIR interpreter for undefined-behaviour detection; execution model, extern function stubs, `cargo miri test`; **Polonius** — the new NLL-successor borrow checker; Datalog-based constraint formulation; the three problem variants (location-insensitive, Naive, DataFrog); comparison with Polonius v2 (a-mir-formality-based); **a-mir-formality** — the formal mechanised model of Rust's type system in PLT Redex; the formality type rules; how it drives Polonius v2 and trait solver work; `rustc_codegen_llvm`: `FunctionCx`; MIR → LLVM IR lowering; ADT/enum layout; trait-object vtable emission; Rust-specific LLVM attributes (`nounwind`, `noalias`, `dereferenceable`, `noundef`); panics and unwinding (`-C panic=abort` vs Itanium EH); LTO (`-C lto=thin/fat`); PGO; cross-compilation and target triples; rustc's vendored LLVM fork and version delta; **Cranelift** — the alternative codegen backend used by Wasmtime and `rustc_codegen_cranelift`; Cranelift IR (CLIF); the compiler pipeline (parsing → legalization → regalloc2 → emission); why Cranelift for fast debug builds; **GCC backend** (`rustc_codegen_gcc`): status and motivation |
| 178 | The Rust Compiler Ecosystem | **LLVM/MLIR Rust bindings**: `llvm-sys` — raw unsafe bindings to the LLVM C API; `inkwell` — safe, idiomatic LLVM IR builder wrapping `llvm-sys`; `iron-kaleidoscope` as a worked tutorial; `melior` — ergonomic MLIR bindings for Rust (Context, Module, OpBuilder, PassManager); `pliron` — MLIR-inspired extensible IR framework written in Rust (trait-based ops, dialect registry, SSA builder); Calyxir — compiler IR for hardware accelerators; **Object-file and debug-info crates**: `object` — unified read/write for ELF, Mach-O, PE/COFF, Wasm, XCOFF; `gimli` — lazy zero-copy DWARF read/write (used by rustc, `backtrace`, `perf`); `addr2line` — address→file:line symbolication via DWARF; **Compiler-infrastructure crates**: `ena` — union-find + congruence closure + snapshot/rollback extracted from rustc, used by Chalk and Polonius; **GPU and ML**: `rust-cuda` — device-side Rust targeting PTX via `nvptx64-nvidia-cuda`; Burn — ML framework with pluggable backends; CubeCL — GPU kernel DSL embedded in Rust (compile-time kernel specialisation, targets CUDA/Metal/Vulkan) |
| 179 | LLVM/MLIR for AI: The Full Stack | The AI compilation hierarchy: PyTorch/JAX eager → `torch.export`/`jax.jit` capture → StableHLO/HLO → MLIR dialects → target ISA; key dialects: `tosa`, `stablehlo`, `tensor`, `linalg.generic`, `vector`, `memref`, `gpu`; quantization: `quant` dialect, INT8/INT4/FP8 lowering; dynamic shapes and StableHLO dynamism extensions; hardware targets: CUDA (PTX via NVPTX), ROCm (HSACO via AMDGPU), NPUs (Apple ANE via CoreML, Hexagon DSP, Edge TPU); inference deployment stack: IREE, TFLite, ONNX Runtime, TensorRT; training vs inference compilation tradeoffs; FlashAttention 2/3 as kernel-fusion case study; end-to-end path: `torch.export` → torch-mlir → StableHLO → IREE dispatch → PTX/HSACO; synthesis across Parts XIX–XXIII |
| 180 | AI-Guided Compilation | MLGO: the `MLModelRunner` abstraction; the RL-trained inliner (feature vector, reward, training loop — cross-ref Ch66); ML-based register allocator eviction advisor; TFLite runtime embedded in LLVM; offline training and online serving model; ML-based cost models for instruction scheduling; Ansor/AutoTVM: search space definition, sketch-based sampler, cost model learning; IREE's ML-based tile size selection; Triton's autotuner (`@triton.autotune`); profile inference with ML; LLM-assisted compiler development: TableGen authoring, LLM-guided fuzzing (EvoFuzz, CovRL-Fuzz); LLVM IR as LLM training corpus; neural superoptimization (Bansal-Aiken); learned middle-end optimization policies |
| 181 | Formal Verification in Practice | **Push-button verifiers**: Dafny (MIT/Microsoft) — pre/post/invariants with Z3 discharge; ergonomics and limitations; LLM-assisted Dafny (96% benchmark pass rate 2026); Verus — Dafny-style verification for Rust, `requires`/`ensures`/`invariant` macros, the `verus!` proc-macro, ghost variables, `Tracked<T>`; F* (INRIA/Microsoft) — dependent types + SMT, the effect system, extraction to OCaml/C/Wasm; HACL* as the existence proof (verified crypto in Firefox NSS, Linux WireGuard); Why3 — meta-platform targeting multiple solvers; **Model checkers**: CBMC for C (bounded model checking); Kani for Rust (AWS, used on s2n-tls and Firecracker); TLA+/TLC/Apalache for protocol specs (AWS DynamoDB, S3); SPIN for concurrency; nuXmv for symbolic model checking; SV-COMP 2026 results (TACAS); **SMT/SAT engines**: Z3 (MIT), CVC5 (BSD), Bitwuzla (bitvector-heavy), Kissat (SAT); **Neural network verification**: α,β-CROWN (multi-year VNN-COMP winner, GPU-accelerated); Marabou (NYU); **Refinement types for Rust**: Flux — liquid-type checker as a Rust compiler plugin, qualifier maps, `#[flux::sig]`; **AI-assisted proof**: Lean Copilot (LLM tactics in Lean 4); LeanDojo (retrieval + training infrastructure); DeepSeek-Prover-V2, Kimina-Prover, Goedel-Prover (open-weight models trained against the Lean kernel via RL); 2026 vericoding benchmarks: Dafny 96%, Verus 44%, Lean 27% |
| 182 | Language Tooling: Parsers, Lexers, and Syntax Trees | **The tooling-vs-compilation axis**: when error-tolerance and incrementality matter more than correctness; **Lexer generators**: Logos (proc-macro, DFA-based, zero-copy, used widely in Rust); **PEG parsers**: Pest (`.pest` grammar files, PEG semantics, built-in error reporting); `peg` (proc-macro inline `peg::parser!` macro); `pom` (PEG combinators via operator overloading, no macros); **Parser combinators**: Winnow (actively maintained nom successor, streaming/partial parsing, built-in error context); `combine` (Parsec-style, zero-copy, streaming); `chumsky` (rich error recovery, built-in Pratt expression parser); **LR/LL parser generators**: LALRPOP (LR(1), `.lalrpop` grammar files, comprehensive book); Parol (LL(k)+LALR(1), dedicated book, `parol-ls` VS Code language server); Grmtools (YACC-style LR, Rust-native, lexxer + parser); **Lossless syntax trees**: Rowan (green/red tree architecture, used by rust-analyzer, preserves all whitespace and trivia); **ANTLR4**: LL(*) grammar syntax, visitor/listener patterns, the ANTLR4 C++ runtime; connecting an ANTLR4 parse tree to LLVM IR generation; **TreeSitter**: incremental GLR parsing; `.js` grammar authoring; the C API; Rust/Python bindings; use cases (syntax highlighting, structural navigation, `ts_query` pattern matching); why TreeSitter is unsuitable for full compilation; nvim-treesitter and LSP coexistence; **Comparison matrix**: hand-written recursive descent (Clang), ANTLR4, TreeSitter, PEG, LR, parser combinators — decision guide by use case |
| 183 | Modern C++ for Compiler Development: C++23, Contracts, and Reflection | **LLVM's C++ baseline and coding standards**: the LLVM coding standards; banned features (exceptions, RTTI/`dynamic_cast`); the monorepo minimum C++ standard (C++17, moving toward C++20); LLVM's own vocabulary types (`isa<>`, `dyn_cast<>`, `cast<>`, `SmallVector`, `StringRef`, `ArrayRef`, `DenseMap`, `LLVM_DEBUG`); the RFC process for standard upgrades; **C++20 in LLVM today**: concepts replacing SFINAE in ADT and type traits; `std::span` vs `ArrayRef<T>` in new APIs; `[[likely]]`/`[[unlikely]]` on hot paths; `constinit`/`consteval`; `std::bit_cast` replacing `memcpy` punning; the LLVM RFC process for C++20 adoption; **C++23 for compiler infrastructure**: `std::expected<T,E>` — design comparison with `llvm::Expected<T>`/`llvm::Error`, migration path; `std::mdspan` — multi-dimensional non-owning array views directly relevant to MLIR MemRef buffer descriptors and linalg tiling; `std::flat_map`/`std::flat_set` — sorted-vector associative containers matching `SmallDenseMap` performance profile; **deducing `this`** (P0847) — explicit object parameter; eliminating CRTP in `OpBase<>`, `PassBase<>`, `PatternBase<>`; concrete MLIR before/after examples; `std::ranges` views composition for IR iteration chains; **C++26 Contracts (P2900)**: `pre`, `post`, `contract_assert` syntax; violation semantics (`ignore`, `observe`, `enforce`, `quick-enforce`) and build modes; applying contracts to LLVM pass APIs — preconditions on `run(Function &, FunctionAnalysisManager &)` that IR is verified, postconditions that preserved analyses are consistent; contracts as complement to (not replacement for) the IR verifier and `AssertingVH`; contracts on `mlir::PatternRewriter` methods; current Clang implementation status (`-fcontracts`, experimental in Clang 22); the WG21 design history and P2900 trajectory; comparison with Dafny/Verus pre/post contracts (cross-ref Ch181); **C++26 Static Reflection (P2996)**: `^T` reflection of types, functions, members; `std::meta::info` as a compile-time handle; `[:r:]` splicers; `std::meta::members_of`, `std::meta::bases_of`, `std::meta::type_of`, `std::meta::name_of`; `std::meta::define_class` for compile-time class synthesis; applications for LLVM/MLIR: auto-generating TableGen descriptor structs from annotated C++ classes, replacing X-macro dialect/op registration tables, static generation of `getOperationName()` and `verify()` dispatch; comparison with Rust proc-macros (cross-ref Ch177/178); current status: EDG reference implementation, Clang experimental P2996 branch; expected WG21 trajectory toward C++26 IS; **C++26 pattern matching (P2688/P1371 status)**: `inspect` expressions; wildcard `_`, binding, type-test `<T>`, guard `if` patterns; structured binding patterns; applications to LLVM — replacing `isa<>`/`dyn_cast<>` chains in instruction visitors, MLIR Op dispatch, type hierarchy traversal; expected C++29 fallback if not C++26; comparison with Rust `match` and MLIR `PatternRewriter`; **The C++/Rust boundary**: why LLVM itself will not switch to Rust; what contracts + reflection would concretely improve in the C++ codebase; the `unsafe` analogy in C++ (undefined behaviour contracts); co-evolution of the two ecosystems through Rust's LLVM bindings (cross-ref Ch177, Ch178) |

## Chapter Plans that were mistakenly placed in TODO list instead of in this doc:

**Ch177 — rustc: Architecture, MIR, and Codegen Backends** (~20 pp)
1. rustc architecture: driver phases → HIR → THIR → MIR → codegen; the Rust compiler dev guide as reference
2. MIR deep dive: `Place`, `Rvalue`, `Terminator`; MIR as Rust's mid-level SSA-like IR; borrow-checker output on MIR; `mir_borrowck` query
3. Miri — the MIR interpreter: UB detection model; memory model (allocation-based, tag-based Stacked Borrows); extern function stubs; `cargo miri test`; limitations vs sanitizers
4. Polonius — new NLL-successor borrow checker: Datalog-based constraint formulation; three problem variants (location-insensitive, Naive, DataFrog engine); relation to the Chalk trait solver; Polonius v2 and a-mir-formality
5. a-mir-formality — formal mechanised model of Rust's type system: PLT Redex notation; the formality type rules; how it drives Polonius v2 and the next-gen trait solver
6. `rustc_codegen_llvm`: `FunctionCx`; MIR → LLVM IR lowering pass; ADT/enum layout; trait-object vtable emission; `dyn Trait` fat pointers
7. Rust-specific LLVM attributes: `nounwind`, `noalias`, `dereferenceable`, `noundef`; why they matter for optimisation
8. Panics and unwinding: `begin_unwind` path; Itanium EH vs `-C panic=abort`; personality function and landing pads
9. LTO: `-C lto=thin` (ThinLTO cache), `-C lto=fat`; PGO (`-C profile-generate`/`-C profile-use`)
10. Cross-compilation: target triples, `--target`, sysroot layout, `build.rs` target detection
11. Cranelift (`rustc_codegen_cranelift`): CLIF IR; the pipeline (parse → legalization → regalloc2 → emission); why it matters for fast debug builds; Wasmtime integration
12. GCC backend (`rustc_codegen_gcc`): status, motivation, libgccjit API surface
13. rustc's vendored LLVM: version delta strategy, custom patches, upgrade process

**Ch178 — The Rust Compiler Ecosystem** (~15 pp)
1. **LLVM Rust bindings**: `llvm-sys` (raw unsafe C API bindings, version feature flags); `inkwell` (safe, idiomatic `IRBuilder` wrapper; `Module`, `Function`, `BasicBlock`, `Builder`); `iron-kaleidoscope` as worked LLVM-in-Rust tutorial
2. **MLIR Rust bindings**: `melior` (Context, Module, OpBuilder, PassManager, Block API); mapping melior to the MLIR C API; `pliron` — MLIR-inspired extensible IR framework in Rust (trait-based Op/Type/Attribute, dialect registry, SSA builder); comparing melior vs pliron design philosophies
3. Calyxir — hardware-accelerator compiler IR: the Calyx language; lowering to RTL via CIRCT; use in FPGA-targeting ML compilers
4. **Object-file and debug-info crates**: `object` — unified ELF/Mach-O/PE/COFF/Wasm/XCOFF read-write; `gimli` — lazy zero-copy DWARF 5 read/write (used by rustc, `backtrace`, Linux `perf`); `addr2line` — DWARF-based address→file:line symbolication (Rust backtrace crate)
5. **Compiler infrastructure crates**: `ena` — union-find + congruence closure + snapshot/rollback extracted from rustc; used by Chalk trait resolver and Polonius
6. **GPU and ML from Rust**: `rust-cuda` — device-side Rust targeting PTX via `nvptx64-nvidia-cuda` target; Burn — pluggable ML framework (WGPU, CUDA, ROCm backends); CubeCL — GPU kernel DSL embedded in Rust (compile-time kernel specialisation, targets CUDA/Metal/Vulkan/WGPU); how Burn uses CubeCL for its fusion engine

**Ch179 — LLVM/MLIR for AI: The Full Stack** (~15 pp)
1. The AI compilation hierarchy: PyTorch/JAX eager → `torch.export`/`jax.jit` capture → StableHLO/HLO → MLIR dialect tower → target ISA
2. Key dialects: `tosa`, `stablehlo`, `tensor`, `linalg.generic`, `vector`, `memref`, `gpu` — roles and handoff points
3. Quantization: `quant` dialect encoding; INT8/INT4/FP8 lowering paths; GPTQ/GGUF integration points
4. Dynamic shapes: StableHLO dynamism extensions; `tensor.dim`/`shape` dialect; IREE's dynamic shape model
5. Hardware target landscape: CUDA (PTX via NVPTX), ROCm (HSACO via AMDGPU), Apple ANE (CoreML), Hexagon DSP (Qualcomm), Edge TPU
6. Inference deployment: IREE (multi-target, VM-based dispatch); TFLite (flatbuffer format, delegate API); ONNX Runtime (execution provider model); TensorRT (engine caching, layer fusion)
7. Training vs inference: throughput vs latency priorities; precision requirements; rematerialization for activation memory
8. Kernel fusion: FlashAttention 2/3 — tiling, softmax fusion, and causal masking as MLIR case study
9. End-to-end: `torch.export` → torch-mlir → StableHLO → IREE dispatch regions → PTX/HSACO — a traced walkthrough

**Ch180 — AI-Guided Compilation** (~12 pp)
1. MLGO architecture: the `MLModelRunner` abstraction; development mode (gRPC to Python server) vs release mode (compiled TFLite model)
2. The RL-trained inliner: feature vector construction, reward signal (size + perf delta), training loop; cross-ref Ch66
3. ML-based register allocator eviction: `RegAllocEvictionAdvisor`; priority model; how to retrain
4. ML-based cost models for instruction scheduling: learned latency/throughput tables
5. Ansor/AutoTVM: search space sketch-based definition; program sampler; learned cost model; relation to MLIR tiling
6. IREE's ML-based tile size selection: the tuning flow; serialised tuning configurations
7. Triton's autotuner: `@triton.autotune`; exhaustive vs evolutionary search; kernel caching
8. Profile inference with ML: synthesising PGO data from code structure (no instrumented run)
9. LLM-assisted compiler development: TableGen authoring, LLM-guided fuzzing (EvoFuzz, CovRL-Fuzz); LLVM IR as LLM training corpus
10. Neural superoptimization: Bansal-Aiken PLDI approach; learned peephole rule synthesis

**Ch181 — Formal Verification in Practice** (~20 pp)
1. Landscape overview: the four quadrants (push-button vs interactive, code vs spec)
2. **Dafny** (MIT/Microsoft): spec language (pre/post/invariant), ghost variables, `decreases` for termination; Z3 discharge; generics and traits in Dafny; limitations; LLM-assisted Dafny (96% benchmark pass rate, April 2026)
3. **Verus** — Dafny-style for Rust: `requires`/`ensures`/`invariant` macros; the `verus!` proc-macro; `Tracked<T>` and `Ghost<T>`; affine types meeting verification; AWS case studies; current maturity
4. **F*** (INRIA/Microsoft): dependent types + effect system + SMT discharge; extraction to OCaml/C/Wasm; HACL* — verified crypto in Firefox NSS, Linux WireGuard; the trusted base; Why3 as F*'s alternative backend target
5. **Why3** — meta-verification platform: WhyML language; targeting Z3, CVC5, Alt-Ergo, Coq; WP calculus; use in CompCert verification cross-ref
6. **Bounded model checking**: CBMC for C (unwinding assertions, pointer analysis); Kani for Rust (AWS, harness model, `kani::assume`/`kani::assert`; used on s2n-tls and Firecracker); comparison with sanitizers
7. **Temporal model checkers**: TLA+/TLC (exhaustive state enumeration) + Apalache (symbolic, SMT-backed); AWS DynamoDB/S3 case studies; SPIN for LTL/CSP concurrency; nuXmv for symbolic CTL/LTL
8. **SV-COMP 2026** (TACAS, Turin): categories and winners; CPAchecker, UAutomizer, Symbiotic; what the competition measures and doesn't
9. **SMT/SAT engines**: Z3 (MIT) — API basics, tactics, quantifier handling; CVC5 (BSD) — arithmetic and strings; Bitwuzla — bitvector/floating-point; Kissat (SAT, CDCL); how verifiers compose these
10. **Neural network verification**: α,β-CROWN (multi-year VNN-COMP winner, GPU-accelerated branch-and-bound); Marabou (NYU, simplex-based); use cases (safety-critical ML, adversarial robustness)
11. **Refinement types for Rust**: Flux — liquid-type checker as rustc plugin; `#[flux::sig]` annotations; qualifier maps; `RefinedBy`; current limitations
12. **AI-assisted proof**: Lean Copilot (LLM suggestions inside Lean 4 tactic mode); LeanDojo (retrieval-augmented proof search, training infrastructure); DeepSeek-Prover-V2, Kimina-Prover, Goedel-Prover — open-weight models trained via RL against the Lean kernel; 2026 vericoding benchmarks: Dafny 96%, Verus 44%, Lean 27%

**Ch182 — Language Tooling: Parsers, Lexers, and Syntax Trees** (~12 pp)
1. The tooling-vs-compilation axis: when error-tolerance, incrementality, and speed matter more than full correctness
2. **Logos** (Rust lexer generator): proc-macro DFA construction; zero-copy token types; `#[regex]` and `#[token]` attributes; performance characteristics
3. **PEG parsers**: Pest (`.pest` grammar files, built-in error reporting, `pairs()` API); `peg` (proc-macro inline `peg::parser!`); `pom` (PEG via Rust operator overloading, no macros); PEG semantics and packrat memoization tradeoffs
4. **Parser combinators**: Winnow (actively-maintained nom successor; streaming/partial parsing; built-in error context and span tracking); `combine` (Parsec-style, zero-copy, streaming); `chumsky` (rich error recovery via `Recovery`, built-in Pratt expression parser, migrated to Codeberg)
5. **LR/LL parser generators**: LALRPOP (LR(1), `.lalrpop` grammar files, the LALRPOP book); Parol (LL(k)+LALR(1), dedicated book, `parol-ls` VS Code language server); Grmtools (YACC-style LR, lexxer+parser suite, Rust-native)
6. **Lossless syntax trees**: Rowan (green/red tree architecture; immutable green tree + mutable red tree with parent pointers; used by rust-analyzer; preserves all whitespace and trivia; comparison with ANTLR4 parse trees)
7. **ANTLR4**: LL(*) adaptive parsing; `.g4` grammar syntax; visitor vs listener patterns; the ANTLR4 C++ runtime (`ANTLRInputStream`, `CommonTokenStream`); connecting to LLVM IR generation; error recovery strategies
8. **TreeSitter**: incremental GLR parsing; `.js` grammar authoring; the C API (`ts_parser_parse`, `ts_node_*`); Rust/Python bindings; `ts_query` pattern matching; why TreeSitter is unsuitable for full compilation; nvim-treesitter and clangd coexistence
9. **Decision guide**: choosing among hand-written recursive descent, ANTLR4, TreeSitter, PEG, LR generators, and combinator libraries by use-case (production compiler, IDE tooling, DSL, one-off tooling, incrementality requirement)

## Part XXVII — Mathematical Foundations and Verified Systems *(~120 pages, 6 ch)* [THEORETICAL]

| # | Chapter | Topics |
|---|---------|--------|
| 184 | Proof Assistant Internals: Lean 4, Coq/Rocq, and Isabelle/HOL | **Lean 4 architecture**: the kernel as the sole trusted base (`Lean.Kernel.check` — does NOT perform unification; that is the elaborator's job); the core dependent type theory (CIC + universe polymorphism + proof irrelevance + quotient types); `Lean.Expr` AST; universe levels (`Level.succ`, `Level.max`, `Level.imax`); the elaboration pipeline: surface syntax → `Syntax` → `Lean.Elab.Term.TermElabM` → `Expr`; `Lean.Meta.M` monad for unification (`isDefEq`) and reduction (`whnf`); the tactic framework: `TacticM`, `omega`, `simp`, `decide`, `native_decide`, `aesop`; the compilation pipeline: `Lean.Compiler.LCNF` (λ-compiler normal form — an ANF-based IR using let-expressions) → IR with explicit reference-counting → C code emission; the experimental LLVM backend (PR #1837, WIP — generates LLVM IR directly, currently limited by inability to inline `lean_object` runtime intrinsics); how Lean 4 bootstraps; Lake build system; `lean4checker` and `Lean4Lean` (a verified Lean 4 typechecker written in Lean, arxiv 2403.14064); LeanDojo and proof search interacting with the kernel (cross-ref Ch181); **Coq/Rocq architecture**: the Calculus of Inductive Constructions (pCIC); the kernel: `check_inductive`, `infer`; the guard checker for fixpoints (structural recursion + size-based termination); `Prop`/`Set`/`Type` universe hierarchy; `SProp` for strict propositions; `Ltac` and `Ltac2` metaprogramming; `SSReflect`; the extraction mechanism: `Extraction Language OCaml|Haskell|Scheme`; how CompCert (cross-ref Ch168) uses extraction to produce a verified OCaml compiler; `native_compute` compiled reduction engine; `Equations` for dependent pattern matching; **Isabelle/HOL architecture**: the metalogic Pure (intuitionistic higher-order logic with schematic variables); HOL built on Pure; the `Thm.t` type as the only route to theorems (LCF approach); Isar structured proof language; `Sledgehammer` — calls external ATPs (Z3, CVC5, Vampire, E, SPASS) and reconstructs Isabelle proofs; how seL4 (cross-ref Ch186) uses Isabelle; Nitpick model finder; AFP (Archive of Formal Proofs); code generation to SML/OCaml/Haskell/Scala; **Comparison**: the three trusted bases; extracted/compiled code entering the LLVM pipeline; connection to Part III type theory (Ch12–15); connection to Ch170 (Alive2) and Ch181 (LLM provers); why CompCert chose Coq extraction, Vellvm chose Coq, and seL4 chose Isabelle |
| 185 | Mathematical Logic and Model Theory for Compiler Engineers | **Proof systems**: natural deduction (ND) for propositional and FOL; the sequent calculus LK; Gentzen's cut-elimination theorem and its computational content (normalisation = reduction — cross-ref Curry-Howard, Ch12); Hilbert-style vs ND vs sequent calculus compared; **First-order logic**: syntax (terms, function symbols, atomic formulas, connectives, quantifiers); Tarski semantics (structures/interpretations, variable assignments, satisfaction); soundness; Henkin's completeness proof; Gödel's compactness theorem; the upward and downward Löwenheim-Skolem theorems; **Incompleteness**: Gödel's first incompleteness theorem (diagonal lemma); the second incompleteness theorem; Tarski's undefinability of truth; what this means for formal verification in practice — why undecidability of Peano Arithmetic is not an obstacle (the theories compilers actually need — Presburger arithmetic, linear arithmetic over rationals — ARE decidable; cross-ref Part XI); **Decidable fragments and the SMT connection**: Church's theorem (full FOL is undecidable); the decidability spectrum: propositional logic (NP-complete, DPLL); quantifier-free linear arithmetic over integers (Presburger, decidable in 2EXPTIME, Omega test — cross-ref Ch70); quantifier-free linear arithmetic over reals (Fourier-Motzkin, LP); equality with uninterpreted functions (EUF, decidable via congruence closure); bit-vector arithmetic (BV, decidable); the DPLL(T) architecture for SMT: CDCL propositional solver + theory solvers for each decidable theory; Nelson-Oppen theory combination; the theories built into Z3 (T_LIA, T_NIA, T_BV, T_UF, T_Arrays, T_FP, T_RE) and how Alive2 (cross-ref Ch170) and Dafny (cross-ref Ch181) encode their VCs as FOL formulas in these theories; **Model theory**: structures and their theories; elementary equivalence and elementary embeddings; the compactness theorem (model-theoretic proof via ultraproducts); quantifier elimination (the model-theoretic perspective — vs the algorithmic Omega test); the model theory of algebraically closed fields and Hilbert's Nullstellensatz (preview of Ch187); **Higher-order logic vs FOL**: the expressivity gap; why HOL is undecidable; why Isabelle uses HOL and Coq/Lean use dependent type theory (cross-ref Ch184); the HOL family (HOL4, HOL Light, Isabelle/HOL); **Connections throughout the book**: Hoare logic (Ch167) as a proof system for programs; separation logic model theory; Z3's theory stack; lattice semantics of abstract interpretation (Ch10) as a model-theoretic structure; the Curry-Howard-Lambek correspondence bridging proof theory, type theory, and category theory |
| 186 | Verified Hardware: CHERI Capabilities and the seL4 Microkernel | **CHERI architecture and capabilities**: capabilities as 128-bit fat pointers — base, length (bounds), permissions bitmask, sealed bit, object-type field, and the out-of-band hardware tag bit (stored outside addressable memory, never forgeable); the CHERI monotonicity guarantee (bounds can only be narrowed, not widened in software); capability-based addressing vs address-space isolation vs MPU; **CHERI ISA variants**: CHERI-RISC-V (the primary research platform, integrated into RISC-V formal Sail spec — see below); Morello (Arm AArch64+CHERI, Neoverse N1 silicon, production hardware used in CheriBSD and Android research); CherIoT (Microsoft, RISC-V32 for IoT bare-metal, combines CHERI with seL4-style capabilities); CHERI-MIPS (original prototype); **CHERI LLVM** (`github.com/CTSRD-CHERI/llvm-project`): address space 200 as the CHERI capability address space (capabilities are non-integral — 64-bit address + 64-bit metadata, external state unlike AMDGPU buffer descriptors); `IntrinsicsCHERI.td` with `int_cheri_cap_bounds_set`, `int_cheri_cap_bounds_set_exact`; CHERI-aware `memcpy`/`memmove` intrinsics (preserve capability metadata); the CheriABI/PCuABI (pure-capability ABI): all memory access and syscall boundaries use capabilities, constraining both userspace and kernel; the Morello AArch64 backend and CHERI-RISC-V backend; `__capability` qualifier and `__intcap_t`; `__builtin_cheri_*` builtins; sub-object bounds enforcement; how CHERI changes the LLVM ABI layer (cross-ref Ch41, Ch96, Ch98); Cornucopia: the CHERI-based temporal memory safety extension using a revocation GC; comparison with ASan/HWASan (cross-ref Ch110, Ch111); **The Sail ISA specification language**: Sail as a typed DSL for ISA specs (bitvector-typed, effect-tracked); the official RISC-V Sail model (used for compliance test suite generation); Sail-to-C extraction (reference simulators); Sail-to-Isabelle extraction (for formal proofs); the CHERI-RISC-V Sail model; connection to LLVM RISC-V backend testing (Ch98–100): Sail model as ground truth; **seL4 microkernel verification**: L4 architecture: IPC as central mechanism; capability-based access control (software object capabilities — unrelated to CHERI hardware capabilities); kernel objects: TCBs, CNodes, VSpaces, Endpoints, Notification objects, Reply objects, Untyped memory; **The seL4 three-level refinement stack**: (1) abstract specification — the system interface as a functional state machine; (2) executable specification — a Haskell-prototype-style functional model; (3) C implementation (~10,000 lines C); two Isabelle/HOL refinement proofs: abstract→executable and executable→C; what the proofs guarantee: functional correctness (C matches spec), integrity (unprivileged code cannot modify kernel state), confidentiality (information-flow policy); what the proofs do NOT cover: hardware defects, timing side-channels, compiler correctness (the C compiler is in the trusted base — CompCert/Ch168 could close this gap); **AutoCorres and the C parser**: Michael Norrish's C parser translates seL4's C into Isabelle `Simpl` language; AutoCorres abstracts from byte-level heap to type-safe monadic form and generates Isabelle proof of the translation; the l4v project (~200,000 lines of Isabelle proof); **Multicore seL4 (WIP)**: the extension to multiprocessor seL4 with spinlock-based concurrency using rely/guarantee reasoning; 2024 status: a formal proof of the CLH lock in seL4 was published; full multicore seL4 proof is on hold pending funding; **CAmkES and deployments**: component architecture metalanguage; capDL (capability description language); DARPA HACMS; F-16 retrofit; seL4 on RISC-V; **seL4 and Rust**: `sel4-sys` crate; Ferrocene (safety-critical Rust, ISO 26262/IEC 61508); **CHERI + seL4 combined**: CherIoT combining hardware capabilities with seL4-style software capabilities on RISC-V32 as complementary orthogonal mechanisms; **Connecting to the book**: the seL4 trusted base gap closed by CompCert (Ch168); CHERI as hardware complement to ASan/HWASan (Ch110-111); Sail as formal ISA ground truth for LLVM backend correctness (Ch98-100) |
| 187 | Commutative Algebra and Its Applications in Compilation | **Ring theory foundations**: commutative rings and ideals; prime and maximal ideals; quotient rings; polynomial rings `k[x₁,…,xₙ]`; the Hilbert basis theorem (every ideal in `k[x₁,…,xₙ]` is finitely generated — Noetherian rings); Hilbert's Nullstellensatz (weak form: the radical ideal correspondence `I(V(J)) = √J`; strong form: varieties and ideals are in bijection for algebraically closed fields); connection to model theory (Ch185 Nullstellensatz as model completeness); **Gröbner bases**: monomial orderings (lex, grlex, grevlex); the multivariate division algorithm; S-polynomials; Buchberger's algorithm; reduced and minimal Gröbner bases; applications to compilation: ideal membership testing (constraint satisfaction); ideal intersection and elimination (projection of polynomial varieties — analogous to Fourier-Motzkin for linear arithmetic, cross-ref Ch70, Ch185); how SMT solvers use Gröbner bases in the theory of nonlinear arithmetic (T_NIA in Z3, cross-ref Ch185); **Algebraic geometry and polyhedral compilation**: toric varieties — varieties defined by monomial maps corresponding to fans and cones; the orbit-cone correspondence; the normal fan of a polytope; the Ehrhart polynomial `ehr(P, t) = |tP ∩ ℤⁿ|` (a polynomial in the dilation parameter `t`) and the Hilbert series of the associated semigroup ring; how Ehrhart theory provides a purely algebraic view of the lattice-point counting that underlies loop iteration counts in polyhedral compilation (cross-ref Ch70–73); connection to the generating-function approach to counting: the Barvinok algorithm as a rational function computation; **Abstract interpretation as algebra**: Galois connections as adjoint functors (cross-ref Ch10); numerical abstract domains as geometric/algebraic objects: intervals (order-1 projection), octagons (unit ball of ℓ∞ norm on the difference-constraint domain), polyhedra (intersection of halfspaces); the widening operator as a semidecision procedure for convergence; abstract domains as completions of the concrete powerset lattice; **Semirings in program analysis — the algebraic path problem**: Tarjan's unified treatment (1981): a dataflow analysis over a graph is an instance of the algebraic path problem `A* = (I - A)⁻¹` where `A` is the transfer-function matrix over a closed semiring; the Boolean semiring (reachability), integer/rational semiring (counting), (min,+) tropical semiring (shortest path / critical path / WCET), (max,+) max-plus semiring (latest firing in timed Petri nets — the well-established connection between tropical algebra and scheduling in discrete-event systems, not instruction-scheduling cost models); MLIR's analysis infrastructure (Ch149) as a closed semiring fixed-point engine; Mohri (2002) for weighted automata as the tropical semiring in action; **Effect systems and graded structures**: Koka's row-polymorphic effects as free commutative monoids; effect algebras (ordered abelian groups) as the semantics of Frank/Effekt; graded monads as effect algebras; connection to Ch14 (effect systems section); **Module theory connections**: free modules over a ring; the Lambek correspondence (the Curry-Howard-Lambek triangle: propositions = types = objects in a Cartesian closed category — cross-ref Ch12 and Ch185); linear logic propositions as modules over a quantale; connection to Rust's affine types and the substructural type-theory of Ch14 and Ch177; **Cox/Little/O'Shea** (Ideals, Varieties, and Algorithms) and **Ziegler** (Lectures on Polytopes) as primary references |
| 188 | Category Theory for Compiler Engineers | **Foundations**: objects, morphisms, identity, composition, associativity; the category of types-and-functions (`Set`), the category of propositions-and-proofs (`Prop`), the category of SSA values-and-rewrites as a 2-category; **Functors and natural transformations**: structure-preserving maps between categories; `Functor` in Haskell/Scala as an instance; natural transformations as the "morphisms of functors"; the naturality square; examples: the list functor, the `Maybe`/`Option` functor, the powerset functor; **Adjoint functors — the central concept**: `F ⊣ G` defined via the unit `η: 1_C → GF` and counit `ε: FG → 1_D`; equivalently via the hom-set bijection `Hom(Fa, b) ≅ Hom(a, Gb)` natural in both arguments; why adjunctions are everywhere: free/forgetful adjunctions, products/exponentials, ∃/∀ quantifiers, syntax/semantics; **Galois connections as adjunctions**: a Galois connection between two posets is precisely an adjunction between the corresponding categories; why every abstract interpreter's `α ⊣ γ` (abstraction/concretization, cross-ref Ch10) is an adjunction; the polyhedral projection Fourier-Motzkin as a quantifier-elimination adjunction (cross-ref Ch70, Ch185); **Monads and effect systems**: a monad is a functor `T` with unit `η: Id → T` and multiplication `μ: T²→ T` satisfying the monad laws; the Kleisli construction — every monad yields a category of effectful computations; examples: the `IO` monad, the `State` monad, the `Error` monad; the monad transformer stack in Haskell as a model for MLIR's nested region + region-based passes; free monads as algebraic effect carriers (connection to Koka/Frank effect handlers); the comonad for comonadic dataflow analysis; **The Curry-Howard-Lambek correspondence**: three-way equivalence: propositions/proofs (intuitionistic logic), types/terms (typed λ-calculus, cross-ref Ch12), objects/morphisms in a Cartesian Closed Category (CCC); the CCC: binary products, terminal object, exponential objects; `Hom(A × B, C) ≅ Hom(A, B→C)` (currying); why this triangle unifies proof theory (Ch185), type theory (Ch12–14), and category theory; linear type theory corresponds to symmetric monoidal closed categories (connection to Rust's ownership, cross-ref Ch177); **Limits and colimits**: products, coproducts (sum types), pullbacks (fiber products), pushouts; initial/terminal objects; the Yoneda lemma: `Nat(Hom(A,–), F) ≅ FA` (representable functors); Yoneda embedding: every category embeds fully-faithfully into its functor category; **2-categories and rewriting**: 0-cells (objects), 1-cells (morphisms), 2-cells (morphisms between morphisms); MLIR rewrite rules as 2-morphisms — the source pattern is a 1-cell, the rewrite is a 2-cell, and confluence = the interchange law (cross-ref Ch149); the `RewriteRule` and `PatternSet` as a globular set; string diagrams for monoidal categories — a 2D notation for compositional proofs; **Fibered categories and dependent types**: the Grothendieck construction; a fibration `p: E→B` models a family of categories indexed by `B`; dependent types (cross-ref Ch13) as a fibration over the category of contexts; **Kan extensions**: left and right Kan extensions as the universal way to extend a functor along another; `Lan` and `Ran`; every concept in category theory is a Kan extension (Mac Lane); applications: the categorical semantics of substitution in type theory; **Toposes**: an elementary topos as a generalization of `Set`; the subobject classifier; the internal language of a topos is higher-order intuitionistic logic (cross-ref Ch185); the effective topos as a model of realizability (Kleene); **References**: Mac Lane (Categories for the Working Mathematician, 2e); Awodey (Category Theory, 2e, Oxford); Riehl (Category Theory in Context, free online); Fong/Spivak (Seven Sketches in Compositionality, free on arXiv); Barr/Wells (Toposes, Triples and Theories) |
| 189 | Denotational Semantics and Domain Theory | **Motivation**: why operational semantics (SOS/big-step, cross-ref Ch11) is insufficient for compositional reasoning — the semantic function `⟦P⟧` as a mathematical object that supports equational laws, program equivalence, and compiler correctness proofs; the Strachey/Scott program (1969–1971): give mathematical meaning to higher-order languages by constructing domains closed under function-space; **Complete partial orders (CPOs) and Scott domains**: a CPO is a poset with a least element ⊥ and least upper bounds (lubs) for all directed subsets; examples: flat domains (discrete types with ⊥ for "divergence"), the natural numbers with ⊥; continuous functions: a function `f: D→E` is continuous iff it is monotone and preserves directed lubs; the key theorem: every continuous function has a least fixpoint `fix(f) = ⊔_{n≥0} f^n(⊥)` (Scott's fixpoint theorem, the generalization of Tarski); the Scott topology: opens are upward-closed and inaccessible by directed sups; **Domain constructions**: lifting `D_⊥` (adding ⊥); product domains `D × E`; sum domains `D + E`; the function-space domain `[D → E]` (continuous functions form a CPO); **Recursive domain equations**: the equation `D ≅ [D → D]` (universal domain for untyped λ-calculus); Smyth-Plotkin's solution via the category of CPOs and embedding-projection pairs (ep-pairs); the D∞ construction; Scott's universally-typed domain; the meaning of `(λx.xx)(λx.xx)` as a fixpoint; **PCF (Programming Computable Functions)**: typed λ-calculus + `fix` + `nat` + `if0`; the standard denotational model in CPOs; adequacy: if the denotation is non-⊥ then the term normalizes (Plotkin 1977); the full abstraction problem: does contextual equivalence coincide with denotational equivalence? For PCF the answer is NO with CPOs alone (Berry's stable functions fill the gap partially); **Game semantics** (Abramsky/Jagadeesan/Malacaria 1994; Hyland/Ong 1992): games as alternating sequences of questions/answers (Opponent=environment, Player=program); strategies as sets of plays closed under prefix and deterministic for Player; the fully abstract game model for PCF; the "definability" direction: every compact strategy is the denotation of a PCF term; **Continuation semantics**: the continuation monad `(A → R) → R`; continuations as the denotation of `call/cc` and non-local control; the CPS transform (cross-ref Ch2, Ch6) as the free continuation monad; the double-negation translation `A → ¬¬A` as the continuation semantics of classical logic in an intuitionistic setting (cross-ref Ch185); why LLVM's switch to CPS-based lowering for musttail is continuation-semantics-inspired; **Predicate transformers and Hoare logic**: the weakest precondition transformer `wp(C, Q)` as a monotone endofunction on the lattice of predicates; Dijkstra's wp calculus; the denotational perspective on Hoare logic — the meaning of `{P} C {Q}` is that `P ⊆ wp(C, Q)` in the concrete domain; the abstract interpretation connection (Ch10): an abstract domain IS a denotational model with approximations; **Powerdomains and nondeterminism**: Plotkin/Smyth/Hoare powerdomains for nondeterministic programs; the angelic (Hoare), demonic (Smyth), and erratic (Plotkin) powerdomains; connection to specification refinement and program algebra; **Process calculi**: Robin Milner's CCS — processes `P`, actions `α`, the operational semantics via labelled transition systems (LTS); bisimulation equivalence `≈` (two processes are bisimilar iff they can always match each other's moves); the π-calculus (Milner/Parrow/Walker 1992): name-passing processes, the mobility principle (channels as first-class values); bisimulation for the π-calculus; session types as a type discipline for the π-calculus (Yoshida/Honda, connection to linear types, cross-ref Ch14 and Ch188); CSP (Hoare 1985): the trace model, failures model, divergences/refusals; FDR4 (Formal Development and Refinement, University of Oxford) model checker for CSP; the equivalence between CSP/CCS/ACP (process algebra); Go channels as an operational-CSP implementation; **Connections throughout the book**: continuation semantics (Ch2, Ch6), abstract interpretation (Ch10), type theory (Ch12–14), category theory (Ch188), session types (Ch14), Hoare logic verification (Ch167), game semantics and proof search (Ch184); **References**: Stoy (Denotational Semantics: The Scott-Strachey Approach); Schmidt (Denotational Semantics: A Methodology for Language Development); Winskel (Formal Semantics of Programming Languages, MIT Press 1993); Milner (Communication and Concurrency, Prentice-Hall 1989); Hoare (Communicating Sequential Processes, Prentice-Hall 1985); Abramsky/Jung "Domain Theory" in Handbook of Logic in Computer Science Vol 3 |

## Part XXVIII — Language Ecosystems, Emerging Targets, and Engineering Practice *(~86 pages, 7 ch)*

| # | Chapter | Topics |
|---|---------|--------|
| 190 | CIRCT: Circuit IR Compilers and Tools | **Architecture**: CIRCT (`github.com/llvm/circt`) — an MLIR-based hardware design toolchain in the LLVM ecosystem; motivation: hardware compilers need the same infrastructure as software compilers (SSA, dominators, passes, verifiers); the `firtool` CLI (analogous to `mlir-opt`); **The CIRCT dialect stack**: FIRRTL (Flexible Internal Representation for RTL, the Chisel IR) — `firrtl.circuit`, `firrtl.module`, `firrtl.wire`, `firrtl.reg`, `firrtl.connect`, `firrtl.when`, memory ports; moore (SystemVerilog import via slang frontend); HW (structural hardware — `hw.module`, `hw.instance`, `hw.output`, port directions); Comb (combinational logic — `comb.add`, `comb.and`, `comb.mux`, `comb.concat`, `comb.extract`); Seq (sequential — `seq.firreg`, `seq.compreg`, clock domains, `seq.read_port`/`seq.write_port` for memories); Handshake (dataflow/elastic — token-passing channels, `handshake.fork`, `handshake.merge`, `handshake.buffer`); Arc (optimizable RTL — arc functions over bitvectors, no side effects, DCE/CSE-able); SV (SystemVerilog emission — `sv.if`, `sv.always`, `sv.assign`, `sv.verbatim`); LLHD (Low-Level Hardware Description — the original research dialect, now mostly Arc); Calyx (HLS intermediate); **The FIRRTL→LLVM lowering pipeline**: Chisel 6 → FIRRTL text → firtool FIRRTL dialect → FIRRTL passes (ExpandWhens, LowerTypes, InferResets, RemoveReset) → HW/Comb/Seq → SV emission or MLIR-based simulation; key transforms: deduplication (module merging by structural hash), grand central views (debug signal injection), inline flatten; **High-Level Synthesis**: the Calyx language and dialect for pipelined HLS; `calyx.component`, `calyx.group`, `calyx.seq`/`calyx.par` scheduling; lowering to statically-scheduled FSMs and then to Seq/Comb; the `dynamatic` HLS tool (ETH Zürich) using CIRCT's Handshake dialect for dynamic scheduling; comparison with Vitis HLS (Xilinx) and Bambu HLS; **MLIR integration**: CIRCT as a downstream consumer of LLVM/MLIR; `OpInterface`/`TypeInterface`/`AttrInterface` usage mirroring MLIR patterns (cross-ref Ch135); the InnerRef mechanism (typed reference to an `hw.module` port or `hw.instance`); CIRCT's type system: `!firrtl.uint<N>`, `!firrtl.sint<N>`, `!firrtl.bundle<...>`, `!hw.inout<i32>`, `!seq.clock`, `!seq.immutable<T>`; **Formal verification with CIRCT**: `circt-bmc` bounded model checking using the `verif` dialect; `verif.assert`, `verif.assume`, `verif.cover` LTL properties; CIRCT's SMT backend (Z3/Bitwuzla integration via the `smt` dialect); **Formal semantics**: ESSENT fast simulation (CIRCT-generated C++); Chisel's Tywaves signal introspection; the `om` Object Model dialect for design metadata; **Academic ecosystem**: chipyard RISC-V SoC generator (FIRRTL-based); FAB (FPGA-as-a-Service Toolkit); NASA core Flight System; **References**: Eldridge et al. (CIRCT: MLIR for Hardware Design, ICCAD 2021); FIRRTL language specification (chisel-lang.org/firrtl); Calyx language specification (calyxir.org); Calyx: Compiler Infrastructure for Accelerator Generators (ASPLOS 2021) |
| 191 | Quantum Compilation: QIR, QUIR, and MLIR Quantum Dialects | **Why quantum compilation is a compiler problem**: quantum programs combine classical control flow with quantum gate sequences; the compiler manages qubit allocation, gate optimization, error correction overhead, and backend-specific pulse decomposition; the full compilation stack: high-level quantum language → gate-level IR → physical qubit mapping → native pulse sequences → hardware; **QIR (Quantum Intermediate Representation)**: defined by the QIR Alliance (Microsoft-led); LLVM IR-based (not MLIR); `qubit*` and `result*` as opaque pointer types avoiding GC; quantum operations as LLVM intrinsics — `__quantum__qis__h__body(qubit*)`, `__quantum__qis__cnot__body(qubit*, qubit*)`, `__quantum__qis__rz__body(double, qubit*)`, `__quantum__qis__mz__body(qubit*, result*)`; qubit allocation: `__quantum__rt__qubit_allocate()` → `__quantum__rt__qubit_release(qubit*)`; adaptive QIR (QIR profile with full LLVM IR control flow conditioned on measurement results — classical feedback loop within a shot); the QIR Alliance specification and conformance test suite; **QUIR (Quantum Unified IR)**: IBM Research, MLIR-based; the `quir` dialect — `quir.declare_qubit`, `quir.call_gate`, `quir.call_defcal` (pulse-level calibration), `quir.measure`, `quir.reset`, `quir.delay`; the `oq3` (OpenQASM 3) dialect for parsing/emission; QUIR's position in IBM's Qiskit compilation pipeline; timing semantics in QUIR (synchronization barriers, duration types); **MLIR quantum research dialects**: Catalyst (PennyLane/Xanadu) — the `quantum` MLIR dialect for JAX-compatible quantum-classical programs; gradient compilation via `gradient.adjoint` and `gradient.backprop`; JIT to LLVM IR via `mlir_quantum_runner`; Intel's `qe-compiler` (MLIR-based); `mlir-quantum` (Sandia NL) research prototype; **Circuit optimization**: the Clifford+T gate set (universal for quantum computation); T-count minimization (Clifford group has polynomial-time classical simulation; T gates are the expensive resource); ZX-calculus as a string-diagram rewriting system for quantum circuits — `ZXlive` and `PyZX` compilers (rewrite rules as 2-morphisms, cross-ref Ch188); circuit-to-native-gate decomposition (basis rotation, Solovay-Kitaev approximation theorem for universality); transpilation: `qiskit.transpile` with routing/layout/optimization; **Qubit mapping and routing**: physical qubit connectivity constraints (heavy-hex topology on IBM QPUs, linear ion traps); the SWAP-insertion problem (NP-hard in general, heuristics: SABRE, TKET routing); **Error correction overhead**: logical vs physical qubits; the surface code — d×d grid of physical qubits encoding one logical qubit, distance d; the T-factory circuit pattern for fault-tolerant T gates; Microsoft's topological qubit roadmap (Majorana-based, 2025 Nature paper); why error correction multiplies qubit resource requirements ~1000×; **Backend targets**: IBM QPU families (Eagle/Heron/Flamingo) via OpenPulse; IonQ Forte (trapped ions); Quantinuum H2; Amazon Braket; simulation backends: AerSimulator (Qiskit, GPU-accelerated via CUDA), qsim (Google, statevector simulation using AVX-512/GPU), cuStateVec; **Noise models**: density matrix simulation; Kraus operator formalism; Lindblad master equation for open systems; shot-based vs exact simulation trade-offs; **Classical–quantum co-design connection**: CIRCT for classical hardware co-design (Ch190); category-theoretic string diagrams for quantum circuits (Ch188); ZX-calculus as a categorical PROP; **References**: QIR Alliance specification (qir-alliance.org); Cross et al. (OpenQASM 3: A Broader and Deeper Quantum Assembly Language, ACM TOPLAS 2022); Coecke/Kissinger (Picturing Quantum Processes, CUP 2017); Amy (Towards Large-Scale Functional Verification of Universal Quantum Circuits, QPL 2018); PennyLane Catalyst documentation |
| 192 | Swift SIL: Ownership, Optimization, and Influence on MLIR | **Motivation**: Clang/LLVM IR has no ownership concepts — you cannot write a pass that enforces "this value is released exactly once"; SIL (Swift Intermediate Language) sits between the Swift type-checker and LLVM IR as a fully type-annotated, ownership-aware SSA IR, enabling ARC optimization, diagnostics, and formal ownership checking; **SIL value ownership**: `@owned` (caller holds a +1 retain, must be consumed exactly once in all paths), `@guaranteed` (a borrowed reference, lifetime is a subset of a surrounding `@owned`), `@unowned` (unchecked weak reference, no refcount), `@inout` (mutable borrow, exclusive access enforced); the linear use requirement for `@owned` makes ARC correctness a data-flow property, checkable without running the program; **SIL instruction set** (`swiftc -emit-sil` / `swiftc -emit-silgen`): `alloc_ref $Foo`, `copy_value`/`destroy_value` (ownership-correct ARC), `begin_borrow`/`end_borrow` (borrow scope), `begin_access`/`end_access` (exclusive access for stored properties — connection to Stacked Borrows, cross-ref Ch177); `apply` (direct call), `partial_apply` (closure capture, produces a thick function value), `open_existential_ref`/`init_existential_ref` (existential boxing); `switch_enum`, `select_enum`; function types: `@convention(thin)` (no context), `@convention(thick)` (closure), `@convention(c)`, `@convention(method)`, `@convention(witness_method)`; `mark_dependence` (lifetime dependency for non-escapable types, Swift 5.9+ `~Escapable`); **SIL pipeline stages**: (1) Raw SIL — directly from the AST, retains/releases explicit, not ownership-normalized; (2) Canonical SIL — after mandatory passes (diagnostics: definite initialization, exclusivity checking, memory lifetime, no-implicit-copy); (3) Optimized SIL — after the ARC optimizer, devirtualizer, function specializer, SIL combiner, stack promotion; (4) Lowered SIL — before IRGen; **Key SIL passes**: the ARC optimizer (global ARC analysis — retain/release pair elimination, loop hoisting of ARC operations, proving that retains are balanced without actually counting); devirtualizer (uses protocol witness tables and class vtables for static dispatch); function specializer (monomorphization for generic types, analogous to Rust's monomorphize — cross-ref Ch177); SIL combiner (instruction combining analogous to `InstructionCombining`); stack promotion (`alloc_ref` → `alloc_stack` when the object does not escape); **Existentials and generics**: protocol witness tables as vtable equivalents; value witness tables for generic layout (stride, size, alignment, copy/destroy functions); `@opened` existentials (opened at runtime with a type variable); the resilience model (library evolution — ABI stability requires that changing struct fields does not break binary compatibility); **Move-only types** (Swift 5.9+ `noncopyable`): `consume` operator, `consuming`/`borrowing`/`mutating` method modifiers; exhaustive consumption checking as a data-flow problem similar to Rust's move checker (cross-ref Ch177); `~Copyable` and `~Escapable` constraints; **SIL's influence on MLIR**: Jordan Rose's 2018 LLVM Dev Meeting talk "Ownership Is Membership" — the conceptual bridge from SIL to MLIR; MLIR's `bufferization.to_tensor`/`bufferization.to_memref` destination-passing model; the Buffer Deallocation pass as an ownership-correctness pass; the `linalg` dialect's destination-passing style for in-place semantics; `mlir::Value` ownership conventions; **IRGen**: SIL→LLVM IR; heap vs stack allocation decisions already resolved; ARC operations lowered to `swift_retain`/`swift_release` runtime calls; witness table calls lowered to function pointers; **References**: Swift SIL Reference (docs.swift.org/compiler/SIL.html); Jordan Rose "Ownership Is Membership" (LLVM Dev Meeting 2018); Swift Evolution SE-0366 (noncopyable generics), SE-0377 (borrow and consume operators), SE-0430 (~Escapable); Groff/Lattner (Swift: A Modern Programming Language for Safety and Performance, 2015) |
| 193 | Julia: Type-Inference-Driven LLVM Specialization | **Compilation model**: Julia's "type-specialization on demand" — when a function is first called with a concrete type signature, the compiler infers types, generates LLVM IR, and caches a `MethodInstance`; identical type signatures reuse the cached compilation; `@code_lowered` (Julia AST), `@code_typed` (typed IR), `@code_llvm` (LLVM IR with optional `optimize=true`), `@code_native` (assembly); this is distinct from AOT monomorphization (Rust/C++) and from JVM JIT (profile-driven); **Julia IR levels**: surface AST → `Meta.@lower` desugared AST → lowered IR (Julia's own SSA-form IR: `Core.CodeInfo`, phi nodes, `%N` SSA values) → typed IR (`Core.Compiler.InferenceState`, types attached to each SSA value) → LLVM IR (`jl_llvm_context`); `Core.Compiler.typeinfer_ext_toplevel` entry point; **Type inference**: abstract interpretation over Julia's type lattice (`Int64 <: Signed <: Integer <: Number <: Any`; `Union{T,S}` as lattice joins; `Const(v)` for constant propagation); `Union{}` as the bottom type; `Core.Compiler.typeinf_code` implementing the Cousot-style fixed-point iteration; inter-procedural inference with inlining driven by type information; **Multiple dispatch and specialization**: a `MethodTable` maps signatures to `Method`s; dispatch selects the most-specific applicable method by subtype relationship; when types are concrete, dispatch resolves statically and the callee is inlined; the ambiguity resolution rules; **LLVM IR generation** (`src/codegen.cpp`): `jl_compile_linfo` → `jl_emit_code` → LLVM `Function*`; `jl_value_t*` as the representation for GC-managed heap objects; boxing/unboxing between Julia's value representation and LLVM primitives; the GC root placement pass (`llvm/lib/Transforms/Scalar/GCRootPlacement.cpp` replaced by Julia's own pass in newer versions); LLVM's GC statepoint and safepoint infrastructure for relocating GC roots through LLVM optimization (cross-ref Ch116); `jl_tupletype_t*` for the method signature; the `@llvm.julia.gc_alloc_bytes` intrinsic; **GC**: generational stop-the-world + incremental collector (1.10+); the tlab (thread-local allocation buffer); `jl_gc_push`/`jl_gc_pop` stack root management; conservative stack scanning (no precise stack maps pre-1.10); **GPU compilation**: `CUDA.jl` and `Metal.jl` — replace the LLVM CPU backend with PTX/Metal targets; `GPUCompiler.jl` provides the backend-agnostic frontend (strips host-only IR, enforces no GC, no dynamic dispatch); `KernelAbstractions.jl` abstracts across CUDA/AMD/Metal; `@cuda` macro transforms a Julia function into a PTX kernel at the typed IR level; connection to MLIR GPU dialect (Ch152) and PTX/NVPTX codegen (Ch101); **Profile-guided optimization** (Julia 1.10+): `--pkgimages` ahead-of-time precompilation with a type profile cache; incremental recompilation when method signatures change; `Revise.jl` for interactive development; **Type instability**: when inference widens the type to `Any` (e.g., dict lookup without type annotation); `@code_warntype` shows red-colored unstable types; the boxing overhead; `@inbounds` and `@simd` pragmas for loop optimization; `StaticArrays.jl` as a zero-cost abstraction via full static specialization; **Automatic differentiation on Julia IR**: `Zygote.jl` (source-to-source AD via the Julia compiler internals); `Enzyme.jl` (LLVM-level AD, cross-ref Ch174 AI-guided compilation); `Diffractor.jl` (JIT-compiled, using Julia's type inference to track tangent types); **References**: Jeff Bezanson PhD thesis (Abstraction in Technical Computing, MIT 2015); Bezanson et al. (Julia: A Fresh Approach to Numerical Computing, SIAM Review 57(1) 2017); Churavy et al. (Diffractor: JIT-compiled automatic differentiation for Julia, 2022); Julia devdocs (devdocs.julialang.org/compiler) |
| 194 | Zig: Comptime Metaprogramming and LLVM IR Generation | **Compilation model**: Zig is an AOT-compiled systems language; the compiler (`src/` in Zig since 0.10, previously self-hosted in C++) has three stages: ZIR (Zig IR) → AIR (Analyzed IR) → LLVM IR or Zig's own backends (x86, arm, wasm, C); `zig build-exe`/`zig build-lib`/`zig build-obj` entry points; `zig build` uses a `build.zig` Zig program as the build system (not a DSL); **ZIR (Zig IR)**: syntactic, unanalyzed — one ZIR instruction per AST node; `src/Air.zig` and `src/Zir.zig`; ZIR is interned for incremental compilation and used by ZLS (Zig Language Server); `zig ast-check` validates ZIR; `zig fmt` uses ZIR; the ZIR instruction set: `call`, `param`, `as_node`, `elem_ptr`, `field_ptr`, `array_init`, `struct_init`, `switch_block`, `try`, `errdefer_code`; **AIR (Analyzed IR)**: typed, monomorphized — the result of semantic analysis, comptime evaluation, and type inference; a typed SSA IR with Zig-level types (`type`, `comptime_int`, `error_union`, `optional`, `struct`, `enum`, `union`, `array`, `pointer`); AIR instructions: `arg`, `alloc`, `store`, `load`, `br`, `cond_br`, `block`, `loop`, `call`, `assembly`, `ret`, `ret_ptr`; the difference from LLVM IR: AIR retains Zig semantics (tagged optional/error/union, `comptime_int` with arbitrary precision); **Comptime execution**: `comptime` qualifier forces evaluation at compile-time (applied to values, parameters, or blocks); comptime parameters enable generics without a separate template syntax — a function `fn max(comptime T: type, a: T, b: T) T` specializes at each call site by the `T` argument; the comptime interpreter in `src/Sema.zig` evaluates ZIR instructions in a compile-time namespace; `type` as a first-class comptime value; comptime reflection intrinsics: `@typeInfo(T)` → `std.builtin.Type` union, `@typeName(T)`, `@hasField(T, "name")`, `@field(val, "name")`, `@typeOf(expr)`; `inline for`/`inline while` for comptime iteration over tuple types or enum fields; comparison with C++ templates (Turing-complete but separate instantiation vs unified comptime evaluation), Rust `const fn`/`const` generics, D `static if`/CTFE; **Error handling**: error types (compile-time-sized closed sets, `error{OutOfMemory,AccessDenied}`); error union `!T` = `E || T`; `try expr` = `expr catch |e| return e`; `errdefer` cleanup on error return; error return traces (each `return error` records the PC, forming a lightweight trace without unwinding — stored as an array of return addresses); **LLVM IR generation** (`src/codegen/llvm.zig`): `toLlvmType` maps Zig types to LLVM types; packed structs and explicit alignment; optionals as `{ T, i1 }` structs (or null-sentinel optimization for pointer types); error unions as `{ error_code: u16, payload: T }`; unions as tagged structs; **Safety builds**: four optimization levels: `Debug` (runtime safety: pointer alignment, integer overflow `@addWithOverflow`, slice bounds, unreachable `@panic`, union tag), `ReleaseSafe` (safety + optimize), `ReleaseFast` (no safety), `ReleaseSmall` (size); `@setRuntimeSafety(false)` per-block opt-out; comparison with Rust debug/release modes (cross-ref Ch177); **Zig as a C cross-compiler**: Zig bundles Clang + LLVM + LLD + `musl` libc, so `zig cc` is a drop-in C/C++ cross-compiler for all targets without a native sysroot; `zig targets` shows all supported triple+ABI combinations; `zig build` can cross-compile Zig, C, and C++ in one invocation; used by projects like TigerBeetle, Bun (JavaScript runtime), and ffmpeg cross-compilation; **C interop**: `@cImport { @cInclude("stdio.h"); }` (C header import at comptime via Clang's AST API); `zig translate-c` for C-to-Zig translation; `extern struct` / `extern fn` for C layout compatibility; **Build system**: `build.zig` as a Zig library program — `std.Build.Step.Compile`, `addSystemHeader`, `addCSourceFile`; lazy build graph; the `std.Build.Cache` hash-based caching; **Connections**: LLVM IR (Part IV), Clang (Part V), LLD (Ch115), sanitizers (Ch111–112), cross-compilation (Ch100); **References**: Zig language reference (ziglang.org/documentation/master); Andrew Kelley "Zig Build System" (LLVM Dev Meeting 2022); Andrew Kelley "A Practical Guide to Applying Data-Oriented Design" (2023); Loris Cro "Zig Comptime" blog |
| 195 | Safety-Critical Toolchain Qualification: DO-178C, ISO 26262, and Ferrocene | **What qualification means**: a toolchain qualification establishes that the tool (compiler, linker, assembler) does not silently introduce errors; without qualification, every compiler output must be manually reviewed or independently retranslated; qualification moves the tool from TCL-3 to TCL-1/TQL-1 (no additional measures needed beyond vendor validation); **DO-178C / DO-330 (avionics)**: DO-178C (Software Considerations in Airborne Systems, RTCA SC-205) — five software levels A (catastrophic) through E (no safety effect); DO-330 (Tool Qualification) — tool qualification levels TQL-1 through TQL-5 mapping to Level A–E software; DO-330 artifacts: Tool Qualification Plan (TQP), Tool Operational Requirements (TOR), Tool Verification Cases and Procedures (TVCP), Tool Accomplishment Summary (TAS); DO-330 TQL-1 requires tests demonstrating that the tool's output matches requirements; AdaCore's GNAT Pro qualified to DO-178C Level A; LDRA TBrun and Polyspace Code Prover as verification tools; **ISO 26262 (automotive)**: Functional safety for road vehicles (IS:2018 edition); ASIL levels A–D (Automotive Safety Integrity Level D is most demanding — single-point fault coverage ≥99%); Part 8 (Supporting processes) governs tool classification; TCL 1–3 (Tool Confidence Level) — a compiler generating dead code is TCL-2 (software hazard possible but detectable); tool qualification by validation test suite approach; **IEC 61508 (industrial/generic)**: SIL 1–4 (Safety Integrity Level 4 is most demanding); covers all safety-related E/E/PE systems (electrical/electronic/programmable electronic); IEC 62304 (software lifecycle for medical devices) cross-references 61508; **Ferrocene (Rust qualification)**: a joint venture of Ferrous Systems + AdaCore; Ferrocene is a downstream fork of the official Rust compiler (rustc + LLVM backend) qualified for ISO 26262 ASIL D and IEC 61508 SIL 4, targeting DO-178C qualification; the Ferrocene Language Specification (FLS) — a separately maintained formal specification of the Rust language semantics (not part of upstream rustc; `ferrocene.dev/en/stable/specification/`); the FerreQual qualification test suite; Ferrocene 25.02 (February 2025): Rust 1.84, new target certifications; qualified targets: `aarch64-unknown-none`, `x86_64-unknown-linux-gnu`, `thumbv7em-none-eabi` (ARM Cortex-M4); the qualification report structure and how it maps the FLS to test cases; **MISRA C:2023**: Motor Industry Software Reliability Association coding guidelines for C (175 rules: mandatory/required/advisory); integration with LLVM/Clang: `clang-tidy` + `clang-sa` cover a subset of MISRA rules; commercial MISRA checkers: LDRA, PRQA/Helix QAC (Perforce), Parasoft C/C++test; MISRA C++ (2023 edition now covering C++17); **MISRA Compliance:2020**: the deviation and compliance workflow; justified deviations, permits, and the compliance matrix; **SPARK 2014 and GNATprove**: SPARK as an analyzable subset of Ada (no recursion by default, no aliasing through access types except limited flow-checked cases, no dynamic dispatch beyond controlled types); GNATprove — flow analysis (data dependency, information flow) + formal proof (VCs discharged by Alt-Ergo/CVC5/Z3, cross-ref Ch181); the `SPARK_Mode` pragma (`On`/`Off`/`Auto`); Bronze/Silver/Gold/Platinum levels of SPARK proof; formal verification of NVIDIA's CryptographyNG firmware in SPARK; **LLVM-specific qualification concerns**: the optimizer's UB exploitation (`instcombine` using `nsw`/`nuw` flags, pointer provenance via TBAA, `undef`/`poison` values) as safety risks requiring `-O0`/`-O1` for qualified builds; why `-fno-strict-aliasing`, `-fwrapv`, `-fno-delete-null-pointer-checks` are mandated; the Alive2 correctness checker as a qualification aid (cross-ref Ch170); SPEC CPU and LLVM's own test suite as qualification regression suites; **Compiler validation standards**: ACM/IEEE-CS SWEBOK; the ACAS X next-generation airborne collision avoidance system as a DO-178C Level A project using formal methods + Isabelle (cross-ref Ch184); DARPA HACMS (Ch186 seL4 link); **References**: DO-178C standard (RTCA SC-205, 2011); ISO 26262:2018 (all parts); IEC 61508:2010; Ferrocene Language Specification (ferrocene.dev); Skoglund/Cederström (Qualification of the GCC Compiler for DO-178C Level A, 2015); Cuoq/Kirchner/Kosmatov/Prevosto (Frama-C: Software Analysis Perspective, 2012) |
| 196 | Cross-Language ABI Interoperability: Binding Generators and UniFFI | **The ABI problem**: two languages can share a single LLVM-compiled binary only if their calling conventions, type layouts, name-mangling schemes, and exception models are compatible; C is the universal interoperability substrate ("lingua franca ABI"); **extern "C" and name mangling**: C++ Itanium ABI and MSVC ABI mangling schemes (cross-ref Appendix H, Ch42–43); Rust `extern "C"` + `#[no_mangle]`; Swift `@_cdecl("name")`; Zig `export fn name`; Python `ctypes.CDLL`; the `objcopy --redefine-sym` escape hatch; **Type layout compatibility**: `#[repr(C)]` in Rust — struct layout guaranteed to match C; `#[repr(transparent)]` — newtype with same layout as inner; `#[repr(packed(N))]` — explicit alignment; Rust niche optimizations that break C interop (`Option<NonNull<T>>` = `*mut T`, safe internally but misread as a nullable pointer from C); Zig `extern struct` (C layout) vs packed `struct` vs `@cStruct`; Swift `@objc` classes vs non-`@objc`; **bindgen** (Rust, official LLVM/Rust project): reads C/C++ headers via libclang (the Clang C API, cross-ref Ch54); `bindgen::Builder` → `.header("foo.h")` → `.generate()` → `write_to_file("src/bindings.rs")`; handling: callbacks (unsafe function pointers), anonymous structs/unions, flexible array members, `__attribute__((packed))` structs, variadic functions, `opaque` types; `allowlist_type`/`blocklist_type`/`allowlist_function` filtering; `wasm32-unknown-unknown` target-specific generation; used in the Linux kernel Rust bindings; **cbindgen** (Rust → C, Mozilla): generates a C/C++ header from a Rust `pub` API annotated with `#[repr(C)]`; `cbindgen.toml` (language C vs C++, include guards, style); used by Firefox, Servo, Mozilla NSS, WebRender; `cbindgen --lang c` vs `--lang c++`; **cxx (David Tolnay)**: zero-overhead Rust/C++ bridge; `#[cxx::bridge] mod ffi { ... }` declares shared types and functions; the cxx build system generates both a Rust source file and a `.cc` glue file; `rust::Vec<T>`, `rust::Str`, `rust::Slice<T>`, `rust::Box<T>` as cross-language types with ownership semantics; `UniquePtr<T>` for C++ heap objects; exception handling: C++ exceptions are caught at the bridge boundary and converted to Rust `Result::Err`; the `cxx-async` extension for bridging `std::future` and Rust `Future`; **Swift/C++ interoperability** (Swift 5.9+): the Swift/C++ Interoperability workgroup; `import ClangModule` bridge; C++ value types usable in Swift without `UnsafePointer`; `SWIFT_SHARED_REFERENCE` and `SWIFT_IMMORTAL_REFERENCE` for C++ reference-counted types; Swift functions callable from C++ via the generated `${Target}-Swift.h` header; **Python FFI**: `ctypes` (`CDLL`, `Structure`, `POINTER`, `c_int`, `c_char_p`, `byref`); `cffi` ABI mode vs API mode (API mode compiles a C snippet — preferred for type safety); `pybind11` and `nanobind` for C++ → Python with RAII and GIL management; the Python C API (`PyObject*`, `Py_INCREF`/`Py_DECREF`, the stable ABI `python3-stable-abi`); **JNI (Java Native Interface)**: `JNIEXPORT`/`JNICALL` marking; `JNIEnv*` method dispatch table; `jni.h` type mapping (`jobject`, `jstring`, `jintArray`); `NewGlobalRef`/`DeleteGlobalRef` for GC roots; JNI overhead (~100 ns per call); Kotlin/Native `cinterop` as a modern replacement on native targets; GraalVM Native Image and the JNI isolation concern; **UniFFI (Mozilla)**: a multi-language FFI framework; input: a UDL (Universal Definition Language) IDL file or Rust `#[uniffi::export]` proc-macro annotations; output: bindings for Python, Kotlin, Swift, and Ruby simultaneously from a single Rust implementation; `uniffi-bindgen generate --library libfoo.so --language swift`; used in Firefox for WebRender, geckodriver, the Nimbus SDK; **WebAssembly Component Model**: the emerging language-agnostic component standard (W3C); WIT (WebAssembly Interface Types) IDL for interface definitions; `cargo-component` for Rust components; `wit-bindgen` for multi-language component glue; `wasmtime` component API; `wasm-bindgen` for JS/Rust (Emscripten + wasm-opt path); **Exception and unwinding interoperability**: Rust panics vs C++ exceptions vs Swift errors vs ObjC exceptions; `extern "C-unwind"` stabilized in Rust 1.71 (allows C++ exceptions to propagate through Rust frames without UB); `catch_unwind` at FFI boundaries; libunwind as the common substrate (cross-ref Ch121); **Calling convention details**: LLVM `CC_X86_64_C`, `CC_ARM_AAPCS_VFP`, `CC_AArch64`; `zeroext`/`signext` attributes for sub-word integer types; `byval`/`sret` for struct passing (ABI-dependent); **Fuzzing FFI boundaries**: `cargo-fuzz` + `libFuzzer` applied to the Rust side of an FFI; `ASAN`/`UBSAN` coverage across language boundaries (cross-ref Ch111–112); `cbindgen` + `bindgen` round-trip correctness testing; **References**: Itanium C++ ABI specification (itanium-cxx-abi.github.io); Rust Reference FFI chapter; bindgen user guide (rust-lang.github.io/rust-bindgen); cbindgen user guide; UniFFI documentation (mozilla.github.io/uniffi-rs); LLVM CallingConv.h documentation |

## Part XXIX — Compiler Tooling, Kernel Integration, and Binary Analysis *(~72 pp, 6 ch)*

| # | Chapter | Topics |
|---|---------|--------|
| 197 | Clang Plugin System | `PluginASTAction`/`PluginASTConsumer`; the `-fplugin`/`-load` mechanism vs `-Xclang -load`; `CompilerInstance` access inside a plugin; `ASTConsumer::HandleTranslationUnit` and `HandleTopLevelDecl`; `RecursiveASTVisitor` in plugins; emitting custom diagnostics (`diag::warn_*`, `FixItHint`); `PragmaHandler` for custom pragmas; `CodeGenPlugin` — IR-level hooks via `CodeGenAction::getModule()`; `DiagnosticConsumer` replacement; `FrontendPluginRegistry::Add<>`; the `attr` plugin for custom C++ attribute parsing (`ParsedAttr`, `Attr` subclass); PCH and modules interaction with plugins; out-of-tree CMake setup (`find_package(Clang)`); testing with `clang -cc1 -load`; comparison with libtooling standalone tools and CIR plugins (cross-ref Ch52); practical examples: custom warning checker, compile-time instrumentation injection |
| 198 | Value Tracking Infrastructure in LLVM | `ValueTracking.cpp` — LLVM's library for reasoning about value properties; `computeKnownBits` and the `KnownBits` struct (per-bit zero/one masks, must-zero, must-one); `isKnownNonZero`, `isKnownNonNegative`, `computeNumSignBits`; `ConstantRange` — a compact integer range abstraction; `ConstantRange` arithmetic: `add`, `sub`, `mul`, `shl`, `lshr`, `ashr`, `urem`, `srem`; wrapping vs non-wrapping arithmetic on ranges; how `InstructionSimplify` and `InstCombine` consume `KnownBits` and `ConstantRange`; `DemandedBits` — which output bits are actually consumed by users; `LazyValueInfo` — demand-driven conditional value inference used by JumpThreading and CorrelatedValuePropagation; `getPointerAlignment` and `isKnownAlignment`; the `SimplifyQuery` context (dominator tree, assumption cache, target library info); integrating value tracking into a custom optimization pass; using Alive2 to fuzz and validate value-tracking-driven transformations (cross-ref Ch170) |
| 199 | llvm-mca: Static Performance Analysis | `llvm-mca` architecture — a static, cycle-accurate simulator using LLVM's own scheduling models without executing code; the `Pipeline` and `Stage` abstraction (fetch, dispatch, execute, retire); `InstrBuilder` mapping `MCInst` to internal `InstrDesc`; the processor resource model from TableGen: `ProcResourceUnits`, `ProcResGroups`, `WriteRes`, `ReadAdvance` (cross-ref Ch82, Ch83); throughput analysis (reciprocal throughput per loop iteration) vs latency analysis (critical path); output views: `SummaryView` (IPC, throughput), `TimelineView` (per-instruction dispatch/execute/retire cycles), `ResourcePressureView` (per-unit utilization), `BottleneckAnalysis` (identifying the critical resource); the `CustomBehaviour` plugin API for target-specific simulation hooks; using `llvm-mca` to validate and tune TableGen scheduling models; performance case studies: vectorized reduction loops, gather/scatter, memory-bound kernels on x86-64 and AArch64; integrating `llvm-mca` into CI for performance regression detection; comparison with Intel IACA, uiCA, and `perf stat`/`perf annotate` |
| 200 | Linux Kernel Compilation with LLVM/Clang | The ClangBuiltLinux project and its upstream testing infrastructure; `CC=clang LLVM=1 LLVM_IAS=1` — enabling the full LLVM toolchain (Clang, LLD, LLVM integrated assembler); kernel-specific Clang flags: `-fno-strict-aliasing`, `-fno-delete-null-pointer-checks`, `-fno-common`, `-fstack-protector-strong`, `-msoft-float`, `-mno-fp-ret-in-387`; **ClangCFI for the kernel**: `-fsanitize=cfi-kcfi` (kCFI — call-site control-flow integrity for the kernel without LTO), `cfi-icall` (requires LTO); `__cficanonical`, `__nocfi`, `noinstr` attributes; **ThinLTO for the kernel**: `LLVM_LTO=thin`; `ld.lld` with `-plugin` for LTO; module split and distributed ThinLTO for large kernel builds; **BPF compilation**: `clang --target=bpf -O2 -g`; BTF (BPF Type Format) generation; CO-RE (Compile Once Run Everywhere) relocations (cross-ref Ch106); BPF global variables and maps; the `bpftool` skeleton generation workflow; **kernel sanitizers via Clang**: KASAN (hardware-tag-based on arm64 with MTE — cross-ref Ch111), KMSAN, KCSAN (cross-ref Ch113); **`objtool`**: stack frame validation, ORC unwinder table generation, noinstr validation; **Android GKI** (Generic Kernel Image) — Clang as the mandated kernel compiler since Android 12; GKI ABI stability requirements; ABI compatibility between Clang- and GCC-built modules; `randstruct` plugin for kernel struct layout randomization |
| 201 | Binary Lifting to LLVM IR | Motivation: decompilation, security analysis (vulnerability research, binary hardening), symbolic execution, binary recompilation for new targets, license compliance auditing; the lifting pipeline: disassembly (Capstone, LLVM MC) → CFG recovery (supergraph, indirect branch resolution) → type recovery → LLVM IR generation; **Remill** (Trail of Bits): each machine instruction semantically defined as an LLVM IR function (`__remill_function_call`, `__remill_return`); lifting x86-64 and AArch64 to LLVM IR with full flag semantics; `remill::InstructionLifter` and `remill::TraceLifter`; the `State` struct encoding register file; **McSema** (built on Remill): lifting ELF/PE binaries; CFG reconstruction via IDA Pro/Binary Ninja/Anvill; CFG protobuf format; **RetDec** (Avast): `retdec-decompiler` → LLVM IR → `llvmir2hll` → C/Python output; Capstone-based disassembler; type reconstruction from debug symbols or heuristics; **Lifting challenges**: indirect branches and computed jump tables; self-modifying code and obfuscation; calling convention inference; `noalias` and provenance in lifted memory accesses; exception handler discovery; C++ vtable reconstruction; **Post-lifting optimization**: running LLVM optimization passes on lifted IR (GVN, InstCombine, mem2reg); round-trip correctness validation with Alive2 (cross-ref Ch170); **Related tools**: B2R2 (binary analysis framework, F# + LLVM); binary-to-MLIR research prototypes; angr/Ghidra p-code as alternative IRs; use cases comparison |
| 202 | Apache TVM: An ML Operator Compiler | TVM's position in the ML compiler ecosystem alongside IREE (Ch162) and XLA (Ch153); **the IR hierarchy**: **Relay** — a typed functional IR for whole-graph optimization (operator fusion, layout transformation, type inference); **TIR (Tensor IR) / TVMScript** — a loop-level IR expressed as Python-embedded `@T.prim_func` and `@R.function` decorators; **Tensor Expression (TE)** — the legacy `te.placeholder`/`te.compute`/`te.Schedule` API; `tvm.build` and `tvm.compile`; **schedule primitives**: `split`, `reorder`, `fuse`, `compute_at`, `vectorize`, `unroll`, `parallel`, `tensorize`; **AutoTVM**: feature extraction from schedule configurations, XGBoost/MLP cost model, transfer learning across hardware targets; **Ansor / Meta-Schedule** (TVM 0.9+): sketch-based search space generation, auto-tensorization, per-target tuning records; **BYOC (Bring Your Own Codegen)**: registering a third-party accelerator backend via `register_func("relay.ext.foo")`; the `ExternalCodegen` pass; **Unity** (TVM 0.14+): unified Relax IR merging graph-level and tensor-level IR; **compilation targets**: CUDA (NVTX path), ROCm (via AMDGPU), OpenCL, Metal, Vulkan, WebAssembly (`wasm32-unknown-unknown`), RISC-V, Arm + Compute Library (ACL); **operator fusion**: Relay's `FuseOps` pass and fusion rules; **microTVM**: bare-metal deployment on Cortex-M and Arduino (no OS, no heap allocator); **runtime**: `PackedFunc`, `Module`, `NDArray`, the `Device` abstraction; comparison with IREE (VM-based dispatch vs TVM direct codegen), XLA (HLO-centric vs TE-centric scheduling), and Triton (tile-level vs operator-level abstraction) |

## Part XXX — AI-First Programming Language Design *(~60 pp, 5 ch)*

Source material: `chapters/part-30-AI-first-PL-design/` contains five research documents (203–207) migrated from an adjacent project. Content is reorganised into five book chapters below, with cross-references to existing LLVM/MLIR chapters and the CLAUDE.md standard structure (H1 title, H2 sections, inline references, chapter summary). The source files must be renamed to `ch203-`... convention and rewritten in reference-book style (no research-manifesto preambles, no section-internal ToCs; direct expert-audience prose with code examples).

**Content mapping** (source files → target chapters):
- `203-AI-era-PL-design.md` (90 lines: framing + maturity matrix) → §§ in Ch203
- `205-language-survey.md` (351 lines: language survey Parts 1–4 + PPL Bridge) → §§ in Ch203
- `204-language-specification.md` (592 lines: formal EBNF + type rules) → Ch204
- `206-unified-pl-capabilities.md` (891 lines: Parts 5–7 + Security + Paradigm Failures) → Ch205 (Part 5) + Ch206 (Parts 6–7 + Security + Paradigm Failures)
- `207-analysis-and-roadmap.md` (1753 lines: Security depth + Reflective + Open Problems + Roadmap) → Ch206 (Security depth + Paradigm Failures detail) + Ch207 (Reflective + Open Problems + Roadmap)

| # | Chapter | Topics |
|---|---------|--------|
| 203 | AI-First Programming Language Design: Principles and Landscape | **Framing — 7 AI-first design pressures** that invert human-PL goals: formal semantics over convention, rich type systems (dependent/refinement) as machine-readable specs, explicit algebraic effects so behaviour is statically knowable, homoiconicity / AST-as-data for trivial generation and manipulation, canonical single representation (lossless AST↔text round-trips), structured typed compiler diagnostics, edit locality (bounded blast-radius module guarantees), property specs (`requires`/`ensures`/`invariant`) as first-class syntax; **G5 type-inference strategy** (the key design decision: infer shapes/effects/capabilities where compiler has complete information from usage; require explicit annotation for specs/invariants, convergence bounds, and security labels — where *intent* is the spec and is not derivable from usage); **Capability tier maturity matrix** (9 tiers with theory maturity 1–5, impl maturity 1–5, prototype horizon: coding-agent-PL 4/3/1–2yr, algebraic effects 5/3/1–2yr, LLM-interaction 3/5/now, transformer-dev 3/3/2–3yr, multi-agent 1/1/5–8yr, SDLC-meta-PL 1/2/5–8yr, security/capability 4/2/2–4yr, PPL bridge 4/4/2–3yr, reflective/self-improving 2/2/research-frontier); **Part 1 — AI-native PLs**: MoonBit (flat structure measurably reducing KV cache misses, mandatory explicit top-level types, semantic sampler enforcing syntactic+semantic constraints at token generation time, MoonBit Pilot integrated code agent with async cloud execution, first-class MCP support, WASM/native output, ICSE LLM4Code 2024 paper); Pel (homoiconic Lisp-style AST = data, minimal unambiguous grammar for constrained generation, piping mechanism for linear composition, built-in natural language conditions evaluated by sub-LLMs, REPeL REPL with Common Lisp-style restarts + LLM helper agents for automated error recovery, static dependency analysis → auto-parallelisation; arXiv 2505.13453); Dana (intent-driven neurosymbolic, LLM + symbolic grounding for deterministic outputs, `reason()` calls with adaptive output types, compositional `|` operators for self-improving pipelines, OpenDXA agent framework, AI Alliance / Aitomatic); **Part 2 — vericoding**: definition (vericoding = LLM-generated formally verified code from formal specs; coined Sept 2025 Beneficial AI Foundation); benchmark on 12,504 formal specs: Dafny 82%, Verus/Rust 44%, Lean 4 27% rising fast; POPL 2026 dedicated Dafny vericoding track; Gartner: 60% new code AI-generated by end 2026; AI-authored code has 2.74× more security vulnerabilities than human-written → vericoding directly addresses; Dafny as verification-aware IL (compiles to C#/Java/JS/Go/Python; LLM writes both impl and spec simultaneously; `Dafny as Verification-Aware IL for Code Generation` arXiv 2501.06283); Lean 4 (propositions-as-types, Mathlib4 as de facto AI formal math standard, startups Harmonic/Aristotle/Logical Intelligence/DeepSeek-Prover-V2); Verus (zero-overhead Rust-native syntax, no separate spec language, `cargo verus`, verusfmt, Verus-aware rust-analyzer fork); **Part 3 — structural AI affinity**: Unison 1.0 (content-addressed AST database not text files, renames are free, MCP server for AI to typecheck code and browse docs without touching text, algebraic effects abilities system, distributed runtime, hash-stable refactoring); Koka (row-polymorphic algebraic effects as the type-level reference implementation for `{Stochastic}`/`{Gradient}`/`{RequiresHumanApproval}` throughout this part — `fun f() : <div,exn,state<int>> int`, effect inference, first-class handlers, LLVM backend, Koka 3.x exploring algebraic effects + dependent types, MSR Leijen 2016); Idris 2 (Quantitative Type Theory — each variable has quantity 0=erased/1=linear/ω=unrestricted; linear types for token budgets `Agent[Budget: 1 Token]` and GPU memory buffers `Tensor[Batch,D]{1}`; capability attenuation as quantity-0 erasure; Brady 2021); **Part 4 — LLM interaction languages**: LMQL (Python DSL for controlling LLM inference, declarative constraint syntax — regex/types/token budgets, "query language for LLMs", ETH Zürich); Guidance (Handlebars-style interleaved generation/control, `{{gen 'field' regex=r'\d+'}}` — template IS the schema, grammar-constrained generation via CFG, stateful multi-turn conversation protocols, Microsoft); Outlines (FSM intersection with LLM token vocabulary → formally correct JSON/regex/grammar-constrained sampling, type-safe LLM output, impossible to emit schema-violating output at runtime, dottxt-ai); DSPy (typed I/O signatures separating what from how, MIPROv2/BootstrapFewShot automatic prompt optimisation, metric-as-oracle connecting to `TestOracle[T]`, composable pipeline as typed function call, Stanford NLP, arXiv 2310.03714); **Bridge — 5 PPL paradigms** from Stan/Pyro/Gen mapped to unified PL requirements: (1) `Dist[A]` as type constructor + `sample : Dist[A] →{Stochastic} A` — Stan's `real<lower=0>` as refinement type; (2) inference-as-algebraic-effect-handler — same model code, HMC/VI/Enumerate handlers swap without model rewrite; Koka row polymorphism in application; (3) Gen trace as first-class typed content-addressed value — two runs with same trace hash are semantically identical; reproducibility-by-type: function without `{Stochastic}` has constant trace hash; (4) compiler-derived gradients — Stan derives all gradients from model spec as compiler pass not library; (5) convergence as refinement type — `converges_to(ε)` annotation on training loops; **WASM as AI-first edge substrate** — MoonBit targets WASM/native, Unison is WASM-deployable, Wasm Component Model implements no-ambient-authority capability system at module boundary, WASI makes OS access capability-gated; cross-references: Ch180 (AI-Guided Compilation), Ch181 (Formal Verification — vericoding), Ch184 (Lean 4/Agda proof assistants), Ch106 (WebAssembly and BPF — WASI/WasmGC connection), Ch80 (content-addressable builds — Unison connection) |
| 204 | The Unified AI-First PL: Formal Language Specification | **Layer 1 — Lexical structure**: no indentation sensitivity (all structure brace-delimited — LLM token generation lacks reliable whitespace control; Python-style indentation creates generation-time ambiguity the semantic sampler cannot resolve); mandatory top-level type annotations enforced at the grammar level (`REQUIRES_TYPE_ANN` token after every function name — parser rejects annotation-free definitions; this is what MoonBit measures as reducing KV cache misses); canonical single formatter, no options (gofmt discipline: fixed pretty-printer; LLM-generated and human-written code produce identical output — no diff noise from style); Unicode operators as first-class tokens (`→`, `⊢`, `Σ`, `Π`, `⟨`, `⟩`, `⊑`) with ASCII aliases (`->`, `|-`, `Sigma`, `Pi`, `<,>`, `<=`); EBNF for whitespace/comments/identifiers (term `Ident`, type `TypeIdent`, dimension `DimIdent`)/literals (integer, float, string, bool)/operators (Arrow, FatArrow, Turnstile, FlowLeq, Pipe, Caret for linearity)/complete reserved keyword table (`fn`, `let`, `effect`, `handler`, `with`, `match`, `requires`, `ensures`, `invariant`, `sample`, `observe`, `staged`, `spec`, `proof`, `sorry`, `decide`, `native_decide`, `Tensor`, `Dist`, `Code`, `Σ`, `Π`, `Trusted`, `Untrusted`, `HighIntegrity`, `Public`, `Secret`); tokeniser note: effect rows tokenise as `LBRACE IDENT COMMA IDENT RBRACE`, security labels `String{Untrusted}` distinguished from set literals by production context; **Layer 2 — Concrete syntax (EBNF)**: module structure `module TypeIdent { TopDecl* }` — imports, function declarations, type declarations, effect declarations, handler declarations, spec declarations; function declaration: `fn Ident TypeParams? '(' Params? ')' ':' Type EffectAnn? Contracts? '{' Expr '}'` — type annotation is mandatory, `EffectAnn ::= '<' EffectList '>'`, `Contracts ::= RequiresClause? EnsuresClause? InvariantClause?`; type declarations: ADTs (`data Name = Ctor1 T1 | Ctor2 T2`), record types, effect declarations (`effect E { op : Param → RetType }`), handler declarations; expression grammar: lambda abstraction, function application (left-associative), `match`/`case`, `let`/`in`, `do`-notation, `sample`, `observe`, `staged`, `splice`, tensor literal syntax, pipeline `|>`; type grammar: dependent function `Π(x:A).B`, dependent pair `Σ(x:A).B`, tensor `Tensor[D₁,...,Dₙ]{precision, device}`, distribution `Dist[A]`, code type `Code[T]` for staged computation, security-labelled `A{L}`, linearity annotation `A^`, effect-annotated function `A →⟨E⟩ B`, effect row `<E₁,...,Eₙ>`; **Layer 3 — Type system**: base rules T-Var, T-Lam (Π-intro), T-App (Π-elim), T-Pair (Σ-intro), T-Fst/T-Snd (Σ-elim); effect rules T-Sample (`Γ ⊢ d : Dist[A]` → `Γ ⊢ sample d : A ⟨Stochastic⟩`), T-Observe (`Γ ⊢ d : Dist[A], v : A` → `Γ ⊢ observe d v : () ⟨Conditioning⟩`), T-Handler (effect row typing for handler installation), T-With (handler wraps computation, effect row discharge); tensor rules T-Tensor (dimension types, precision type, device type), T-Grad (gradient type: `grad : (A →⟨Gradient⟩ B) → A → (B → A)` in VJP form); security rules T-IFT (`String{Untrusted}` cannot flow to `{Trusted}` position without sanitizer), T-LevelPoly (level-polymorphic functions); linear rules T-Linear (quantity-1 variables consumed exactly once, `^` annotation enforced by split context); **Layer 4 — Operational semantics**: big-step reduction `e ⟹ v` for core terms; beta/delta reduction; `match` expression semantics by constructor matching; handler reduction — when effect operation `op(args)` encountered, matched against handler clause, continuation `k` passed to handler body; staged evaluation — `staged { e }` blocks deferred to compile time, `splice e` substitutes value; security label propagation (labels are phantom — erased at runtime after type checking); **Layer 5 — Metatheory**: Progress (well-typed closed term is either a value or can step); Preservation (stepping preserves type); Soundness of effect tracking (well-typed program never performs an effect not in its declared effect row — proved by structural induction over derivation); Non-interference for IFT (no `{Untrusted}` datum reaches a `{Trusted}` position without an explicit sanitizer function — proved by label monotonicity); Subject reduction for handler installation (installing a handler for effect `E` reduces the effect row by `E`); **Layer 6 — Implementation plan**: Phase 0 — Lean 4 reference interpreter (~3–6 months, 2–3 people): kernel + elaborator for Layers 1–3, correctness by construction, soundness argument in Lean 4's own type system; Phase 1 — LLVM IR backend via StableHLO (Koka's LLVM backend as template; named tensor types → HLO shapes; `{Gradient}` effect → XLA `dot_general`/`gather`/`scatter`; 6–12 months); Phase 2 — tensor type checking with MLIR shape analysis; Phase 3 — IFT label inference via dataflow; compiler connections: Layers 1–2 → lex/parse pipeline (cross-ref Ch06 Lexical Analysis, Ch07 Parsing Theory), Layer 3 → type inference/HM algorithm W (cross-ref Ch13 Polymorphism and Type Inference), Layer 4 → operational semantics (cross-ref Ch167 Operational Semantics and Program Logics), Layer 6 → LLVM IR backend (cross-ref Ch81 Backend Architecture, Ch14 Advanced Type Systems), MLIR target (cross-ref Ch129–Ch135 MLIR Foundations) |
| 205 | Transformer Model Development PLs | **Why Python frameworks solve the wrong problem**: PyTorch (imperative `nn.Module`, legacy autograd API, dynamic graph is human-friendly but compiler-hostile, 10 years of accidental complexity), JAX (purest design: pure functions, `jit`/`grad`/`vmap`/`pmap` as composable transforms; retreated to Python-embedded Pallas for hardware kernel access to avoid losing human users), TF/Swift for TF (Swift for TF 2018–2021 had right instincts — systems PL with first-class AD — but predated coding agents; without LLMs the learning curve killed adoption), Triton/CuTile (Python DSLs for GPU kernel authoring; constrained Python, not a new PL; tiles expressed as decorators; still require human understanding of memory hierarchy), Burn (Rust DL framework with memory safety but no native dependent tensor types, viable for inference, training support maturing), Ludwig (YAML-to-PyTorch DSL, not a PL); **fundamental opportunity**: AI as primary author eliminates human ergonomics as a design constraint — unlocks a different design space; **production serving demands** that motivate specific design considerations: vLLM (PagedAttention, continuous batching → dynamic shape handling without 10⁵× recompilation overhead), llama.cpp (Int4/Int8/BFloat16 as first-class precision types not casts), TensorRT-LLM (fused attention + FFN as compiler-derived not manual annotation), Triton Inference Server (StableHLO/MLIR as the convergence handoff IR), SGLang (structured generation as first-class type); **10 design considerations for the transformer development tier**: (1) named typed tensor dimensions — `Tensor[Batch, SeqLen, DModel]` as compile-time type; shape mismatches are type errors not runtime crashes; eliminates string einsum specs and `dim=` arguments; AI generates shape-correct code by construction; (2) first-class AD as type-level transform — `grad : (A →{Gradient} B) → A → (B → A)` in VJP form; `{Gradient}` effect tracked in type; POPL 2026 "Nominal Semantics for First-class AD"; not a library function but a compiler-checked transform; (3) parallelism strategies as type annotations — data/tensor/pipeline/sequence parallel as type qualifiers or effect qualifiers; compiler derives all-reduce/all-gather/scatter patterns; consistency checked at compile time; AI annotates parallelism intent, compiler verifies semantics; (4) device and precision as types — `Tensor[CUDA:0, BFloat16, Batch, SeqLen, DModel]`; device mismatches and precision mismatches are type errors; memory tier (HBM/SRAM/registers) as a type for kernel-level programming; mixed-precision rules are typed coercions with explicit promotion schedules; (5) kernel fusion as compiler-derived property — FlashAttention-style I/O-aware tiled attention currently hand-coded; AI-first PL: high-level operator composition → compiler schedules execution; Halide demonstrated this separation for image pipelines; no equivalent yet for transformer kernels at production scale; (6) stochasticity as algebraic effect — dropout/weight initialisation/stochastic depth/speculative decoding sampling as `{Stochastic}` effect tracked by type system; reproducibility as compiler guarantee: function without `{Stochastic}` in effect row is deterministic by construction; (7) activation checkpointing as type annotation — `@recompute` or `Tensor[Recomputed, ...]`; compiler optimises memory/compute trade-off across whole model; (8) Einstein index notation as first-class syntax — `q[b,h,s,d] * k[b,h,n,d] → r[b,h,s,n]` type-checked against named dimension types; Dex's "higher-order dependently-typed Einstein summation"; eliminates string-embedded `torch.einsum` DSL; (9) hardware portability via MLIR — StableHLO/Linalg dialects as compilation backend; hardware-specific annotations `@target(cuda, wmma)` as escape hatches; Mojo's architecture is strongest here; (10) LLM-legible compact DSL for kernel authorship — compact enough for AI to write but expressive enough to specify tiling, memory layout, and unrolling; the semantic sampler applies to kernel DSL syntax; **language/framework survey**: Mojo (MLIR-backed systems PL designed for AI/GPU programming; MAX Engine production deployment; 4× PyTorch on FLUX.2 benchmarks; Apache 2.0 open-source kernel library; Modular compiler stack: Mojo → MLIR → LLVM IR; `@value`, `@register_passable`, `@always_inline` decorators; strict ownership semantics), Dex (typed A-normal form, AD as a core semantic primitive not a library, rank-polymorphic functions, tabulated arrays as functions `(Fin n → Float)` — type-level array indexing, `jaxpr`-style functional representation, Harvard NLP; connections to Koka's AD effect model), Futhark (APL-inspired purely functional GPU PL; auto-parallelism derived from pure semantics; `#[sequential]`/`#[parallel]` annotations; explicit SOAC (Second-Order Array Combinators); DIKU), Exo 2 (exocompilation: programmer schedules externally-defined hardware primitives; separates algorithm specification from schedule (loop ordering, memory layout, vectorisation) as explicit structural parameters; `exo_proc` policy language; connects to TVM's schedule model — Ch202), Clef (type-driven tensor computation, shape inference from dimension type variables), Burn (burn-jit JIT-compiled backend, burn-wgpu WebGPU backend, ndarray CPU backend, quantisation support in Rust), LLM-driven kernel generation (AlphaCode/Copilot for CUDA+Triton — AI writes kernel code in these existing DSLs; semantic sampler applied to Triton syntax); **PPL Bridge in the transformer context**: `Dist[A]` → stochastic loss `Dist[Float]` at training time; inference-as-handler → swap training regime (HMC hyperparameter search, VI embedding, exact enumeration for discrete latents) without model rewrite; trace-as-typed-value → deterministic reproducible training runs by content-hash; compiler-derived gradients → gradient derivation as compiler pass over `{Gradient}`-effect functions; convergence-as-refinement → `converges_to(ε)` annotation on learning rate schedules type-checked against convergence theory; cross-references: Ch163 (Triton GPU kernels), Ch162 (IREE deployment), Ch179 (LLVM/MLIR for AI full stack), Ch180 (AI-Guided Compilation — MLGO and Ansor), Ch64 (Vectorization deep dive), Ch102 (NVPTX/CUDA path), Ch103 (AMDGPU/ROCm), Ch202 (Apache TVM — Ansor/Meta-Schedule connects) |
| 206 | Multi-Agent PLs, AI-First SDLC, Security, and Paradigm Failures | **Part 6 — Multi-agent coordination PL**: 10 design considerations — (1) multiparty session types for typed choreographic handoffs, (2) choreographic compilation to endpoint projection (global protocol → per-agent local programs), (3) probabilistic session types for LLM non-determinism — open research problem (3–5 year horizon), (4) linear resource budgets for token tracking `Agent[Budget: N Tokens]`, (5) `(p, δ, k)`-satisfaction probabilistic contracts and the Drift Bounds Theorem `D* = α/γ`, (6) semantic intent as an ML-derived type not just message format, (7) capability-typed agent instantiation `Agent[Tools:{search, code_exec}]`, (8) typed capability delegation `T1 ⊇ T2` — attenuation, (9) actor-to-agent topology mapping (each agent is an autonomous actor with typed message interface), (10) `@branchpoint` for structured LLM non-determinism exploration; **multi-agent survey**: Choral (JVM choreographic programming — endpoint projection from global protocol to per-party Java libraries, distributed data types `T@(A,B)`, higher-order choreographies as first-class values, deadlock-free by construction; gap: deterministic strongly-typed participants, no probabilistic output or token budget model), HasChor/MultiChor (Haskell monadic DSL — location polymorphism parameterises choreographies over node names, census polymorphism `N` participants as type variable for LLM fan-out, ICFP 2023 arXiv 2303.00924; MultiChor arXiv 2406.13716; gap: Haskell-only, no LLM-specific constructs), Formal-LLM (pushdown automaton supervision at token generation time — PDA defines valid plan syntax; invalid tool-call sequences structurally impossible to generate; 50%+ improvement on planning benchmarks; arXiv 2402.00798; gap: hand-crafted PDA per domain, no multi-turn session state evolution), EnCompass (`@branchpoint` marks LLM non-determinism branch locations; search strategy — beam search, MCTS, best-of-N — as a separate structural parameter; NeurIPS 2025, 80% reduction in search implementation code, arXiv 2512.03571; gap: Python decorator framework, no formal type system for branchpoints), ABC/AgentSpec (probabilistic `(p, δ, k)`-satisfaction: contract satisfied with probability ≥ p, drift ≤ δ over k evaluations; Drift Bounds Theorem: `D* = α/γ` given recovery rate γ > α; AgentAssert <10ms runtime overhead; 88–100% hard constraint compliance across 7 models and 6 vendors on AgentContract-Bench 200 scenarios; LLM-generated rules 95.6% precision; arXiv 2602.22302, ICSE 2026 arXiv 2503.18666; gap: runtime not compile-time enforcement), Semantic Consensus (six-layer middleware: Process Context, Semantic Intent Graph, Conflict Detection Engine, Consensus Resolution Protocol, Drift Monitor, Process-Aware Governance; 100% workflow completion vs 25.1% baseline on AutoGen/CrewAI/LangGraph; MCP and A2A compatible; arXiv 2604.16339; gap: middleware not language, ML-derived intent types not formal types); **Part 7 — AI-first SDLC meta-PL**: 8 design considerations — (1) typed work items `Task[T]{requires, ensures}` where T is the spec type; (2) semantic VCS — commits as type-checked spec deltas `ΔSpec[T]` not text diffs; (3) type-derived deployment manifests — resource requirements as typed formulas not YAML guesses; (4) proof-gated CI — merge gate discharges a Dafny/Lean proof obligation; (5) `{RequiresHumanApproval}` as an algebraic effect propagating through the call graph; (6) continuous `drift : Impl[T] → Spec[T] → DriftReport` check on every commit; (7) content-addressed build graph (Unison model extended to build artefacts); (8) typed configuration language with design-by-contract; **SDLC survey**: Pijul (patch-theory-based VCS — commutative patch composition formally justified; `darcs`-style theory; no merge conflicts by construction), Dhall/CUE/Pkl/Nickel (typed configuration languages: Dhall — total functional language guaranteeing termination, no Turing-completeness; CUE — lattice-based unification, all configs must be valid CUE values; Pkl — JVM-based, object-oriented, Gradle integration; Nickel — gradual typing + design-by-contract for resource formulas `service.memory = tensor_batch * seq_len * d_model * 2`), NixOS/Flakes (byte-for-byte reproducible builds, deterministic `deploy(code, config)` as a function), Dagger (typed CI pipelines in 9 SDKs — Go/Python/TypeScript/Java/PHP/Elixir/Rust/C#/Zenith; content-addressed caching; runs locally = runs in cloud; replaces GitHub Actions YAML), Buck2/Shake (typed incremental build graphs; Buck2 at Meta scale with single dep graph; Shake monadic dynamic deps for data-dependent builds), SWE-agent/OpenHands (autonomous PR resolution from GitHub issues; OpenHands 87% same-day resolution; NeurIPS 2024 SWE-bench validated); **Security and Capability Safety**: AI agent generating and executing code is a powerful attack surface; two complementary language-level mechanisms: (1) object capability languages — authority as first-class values, no ambient authority; (2) information-flow type systems — security label static enforcement; **Pony** (reference capability system: `iso`/`trn`/`ref`/`val`/`box`/`tag`; data races are type errors; `iso` = structurally identical to linear type = token budget tracking; actor model = multi-agent topology; generated code creating data races is a type error; OOPSLA 2015); **Principle A — E language no-ambient-authority** (every object IS a capability; no `File.open(path)` — only `file_handle.write()` where `file_handle` was explicitly passed; MCP tool handles as capability references in AI agents; promise pipelining maps to typed agent pipeline composition; `fn generate_code(spec, compiler: Cap{Compile}, fs: Cap{FileRead})` — {Compile, FileRead} in effect row; erights.org; Capability Myths Demolished Miller/Shapiro/Tribble); **Principle B — Jif information-flow labels** (Decentralised Label Model: security labels as type annotations; type system rejects `{Untrusted}→{Trusted}` flows without explicit sanitizer; LLM inputs carry `{Untrusted}`, external-effect outputs carry `{Trusted}`; prompt injection = type error structurally; multi-agent trust delegation `Agent[trust: A]` on messages; Myers & Liskov SOSP 1997; Cornell Jif project); **Principle C — Flow Caml level polymorphism** (`sort : 'a list{'l} → 'a list{'l}` works at any security level `'l`; effect-level polymorphism: agent function whose security label is a type variable, instantiated at `{Public}`/`{Private}`/`{HighIntegrity}` without rewriting; `{RequiresHumanApproval}` = `{HighIntegrity}` label on algebraic effect row; level polymorphism makes it composable; INRIA, ESOP 2003); ideal: Pony ref-caps + E no-ambient-authority + Jif DLM + Flow Caml level polymorphism, integrated into the algebraic effects type system; **Paradigm Failures Revisited** — 9-row table: No Silver Bullet → *reversed* by Req2LTL (88.4% NL→LTL, 100% syntactic correctness) + FM-ALPACA/FM-BENCH (18k+ instruction pairs) + VERINA (189 Lean/Dafny/Verus challenges); Round-trip SRS→HLD→Code → *dissolved* by spec-as-source-of-truth: `refine: Spec[T] → Architecture[T]`, `drift: Impl[T] → Spec[T] → DriftReport` eliminates round-trips; Waterfall → *superseded* by AI spec checkpoints replacing sprints; Agile → *superseded* by machine-speed bolts (ceremonies dissolve); Formal methods (Z/B/VDM) → *reversed* by LLM-generated formal specs; BDD/Gherkin → *enhanced* to human interface for machine-generated formal assertions; UML round-trip → *reversed* by AI-derived all-views from formal architecture + drift detection; Software Factory/MDA → *reversed* by LLM-generated idiomatic code + content-addressing removes generated-vs-maintained distinction; KISS → *reframed* to "Keep Invariants Simple, Statically verified" — formal decidability replaces human readability as simplicity criterion; **Near-term actionability** — Start Here table: vericoding → Dafny CI step for one security-critical function; transformer PL → Phase 0 Lean 4 reference interpreter (2–3 people, 3–6 months); multi-agent → Phase 0 HasChor 2-agent choreography encoding LLM non-determinism as `Maybe`; SDLC → Phase 0 Dagger + Dafny proof-gated CI; security → MCP tool handles as Pony-style capability objects; PPL → NumPyro for `{Stochastic}` validation; agent coordination → ABC/AgentAssert + DSPy; Phase 0 sketch for Part 6 (HasChor 2-role CodeGen→Verifier protocol, `Maybe` non-determinism, `StateT TokenBudget`, `Branchpoint` ADT; validates which session type properties hold immediately); Phase 0 sketch for Part 7 (Dagger + Dafny + Nickel: Dafny merge gate, Dagger typed CI, Nickel typed resource formulas, `{RequiresHumanApproval}` as Dagger Slack-blocking function); production-ready-now table (Dafny, Verus, Mojo, Dagger, Nickel, NixOS, Buck2, MoonBit, OpenHands); cross-references: Ch181 (Formal Verification — Dafny/Verus/Lean 4 in depth), Ch184 (Proof Assistants — Lean 4 MetaM), Ch14 (Advanced Type Systems — linear types, effect systems, session types), Ch188 (Category Theory — categorical semantics of session types), Ch196 (Cross-Language ABI — WIT component model interfaces = typed capabilities) |
| 207 | Reflective Code, Open Problems, and Build Roadmap | **Reflective, homoiconic, and self-improving code** — framing: 3 required properties: (1) code is data (homoiconicity — the code AST is a value in the language, manipulable by functions in the same language); (2) reflection without a separate API (Maude META-LEVEL principle: `describe` is an ordinary function call, not a separate reflective meta-interface); (3) `patch` = self-modification with formal invariant preservation (the hardest open problem: `patch : MetaModel[P] → ΔSpec[T] → {patch, verify} Option[MetaModel[P']]` must preserve all type-level invariants); **Paradigm A — Lean 4 as primary substrate**: `MetaM` monad for proof state manipulation and macro elaboration; `TacticM` for tactic-mode interactive proofs; `quote e`/`unquote e` as homoiconic operators producing/consuming `Expr` values; `decide`/`native_decide` proof-by-computation (kernel-checked); `Lean4Lean` (Carneiro arXiv 2403.14064) as self-verification; AlphaProof (Google DeepMind, Nature 2025) demonstrating type-checker-as-adversarial-oracle: the formal kernel provides a zero-false-positive reward signal for RL; DeepSeek-Prover-V2 recursive subgoal decomposition: `Spec[T]` must support hierarchical decomposition to subtasks (`Spec[T] → [Spec[T₁], ..., Spec[Tₙ]]`); LeanCopilot (74.2% on Mathlib4 proofs; MCP-exposed type-checker is the LLM–kernel integration point: AI proposes, kernel validates, no AI output enters trusted base); **Paradigm B — same-language metalevel (Maude/rho-calculus)**: Maude META-LEVEL — `upTerm`, `downTerm`, `metaApply` are ordinary Maude functions operating on terms; no separate reflection API; the same equational logic that defines object-level programs also defines meta-level programs; `describe` is a function call; rho-calculus — `quote` and `unquote` as type primitives; capability-gated code mobility: agent-to-agent code transmission is capability-checked; **Paradigm C — staged compilation**: Julia `@generated` for multi-level dispatch, `@code_llvm`/`@code_typed` as inspect-at-stage functions (cross-ref Ch193); Racket `define-syntax`/`define-for-syntax` — DSL type rules as typed data in MetaModel; Pharo Smalltalk live mutable MetaModel — hot-swap with invariant preservation (the `patch` analogue in production); unified PL requirement: `staged fn` for kernel generation (`inspect[stage]` for each compilation tier); **Paradigm D — bootstrapped AI self-improvement**: AlphaVerus (tree search for Verus proofs, bootstrapped verified Rust code generation — first production AI-assisted formally verified systems code at this scale); SICA (self-improving coding agent rewrites its own source files, April 2025 — first language-level self-modification system); **Open Design Spaces**: G1 typed `LLMCall` primitive — must simultaneously have: `Out: Schema` FSM constraint (Outlines), `{Stochastic}` handler-swappable inference (Koka/NumPyro), `Budget[Linear]` token tracking (Idris 2 quantity), IFT labels `{Untrusted/Trusted}` (Jif); no existing PL defines all four simultaneously; G2 type-inference strategy — when to infer vs. when to require annotation: infer shapes (tensor dims from construction), infer effects (Koka row inference), infer capabilities (Pony ref-cap inference from usage); annotate specs/invariants (Dafny postconditions — intent not derivable from usage), convergence bounds, security labels; G3 multimodal I/O types — `Image[W,H,Format]`, `Audio[SampleRate,Channels]`, `Document[Schema]`, `Video[FPS,W,H]` as first-class type parameters on `LLMCall`; no existing PL formalises multimodal typed I/O at the LLM interface level; G4 probabilistic session types — combining multiparty session types with `(p, δ, k)`-satisfaction semantics for LLM participants whose outputs are probability distributions over valid continuations; open type theory problem; 3–5 year horizon; G5 typed code-delta VCS — `ΔSpec[T]` as the commit type; requires semantic equivalence checking across AST versions (decidable only for restricted sublanguages); Unison model extended with type-indexed deltas; G6 convergence refinement types — `converges_to(ε)` annotation requiring formalisation of optimisation dynamics (continuous mathematics) in a type system (discrete formal logic); bridges functional analysis and dependent type theory; G7 semantic consensus as static type — `SemanticIntent[P]` as a formal type capturing what agent P is trying to accomplish; requires ML-derived intent models to be type-checked; hardest open problem (possibly undecidable in general); **Part 8 — Build Roadmap**: difficulty-by-component matrix (tensor type checker: medium; AD as type-level transform: medium-hard; probabilistic session types: hard; typed patch primitive: research; semantic consensus type: very hard / research frontier); Phase 0 (3–6 months, 2–3 people): Lean 4 reference interpreter for single-layer attention — kernel + elaborator for named tensor types + `{Gradient}` effect, written soundness argument, validates type theory before committing to a production compiler; Phase 1 (6–12 months): StableHLO backend — named tensor types → HLO shapes; `{Gradient}` effect → XLA `dot_general`/`gather`/`scatter`; Phase 2 (~12 months): schedule language (algebraic `split`/`reorder`/`compute_at` as type-indexed transforms, Exo 2 as reference); Phase 3 (~12–18 months): multi-precision and device placement (4 primary device types, precision coercion typed rules); Phase 4 (12 months): effect system integration (`{Stochastic}` + `{Gradient}` + `{Parallel}` composition, NumPyro handler as reference); Phase 5 (3–5 years): multi-agent coordination (proof-of-concept HasChor choreography with typed LLM handoffs); key risks: type theory ahead of compilers (researchers propose type systems that no compiler can implement efficiently), Python ecosystem inertia (2M+ PyTorch users), benchmark overfitting (vericoding benchmarks may not capture real-world spec quality), academic → production gap (Swift for TF failed this transition); why 2026 is the inflection point: LLM vericoding reaching 82%+ pass rates for Dafny; Mojo MAX Engine in production; AlphaProof Nature 2025 establishes type-checker-as-oracle; POPL 2026 formal AD session; **Synthesis**: all 7 tiers of the unified AI-first PL as layers of the same type-theoretic foundation — same `Spec[T]` → `RefinementChain[T]` → `Impl[T]` pipeline with AI at each derivation step; `drift` as the continuous consistency operator replacing human-maintained traceability matrices; the trust chain from formal proof (Lean 4 kernel) through extraction/compilation to LLVM binary (Alive2 Chapter 170 and Vellvm Chapter 169 close the remaining gap); cross-references: Ch184 (Lean 4 MetaM — §184.1.6/1.7), Ch180 (AI-Guided Compilation — MLGO connects at the decision-making-in-compiler level), Ch181 (Formal Verification — vericoding context for Dafny/Lean/Verus), Ch163 (Triton — transformer kernel abstraction level), Ch14 (Advanced Type Systems — linear/affine types, effect systems, dependent types), Ch185 (Mathematical Logic — SMT for type checking VCs) |

## Appendices *(~95 pages, 8 appendices)*

| # | Appendix | Contents |
|---|----------|----------|
| A | LLVM IR Quick Reference | Every instruction; full attribute reference; full intrinsic reference; metadata reference; the textual grammar |
| B | MLIR Dialect Quick Reference | Every in-tree dialect; one-page summary cards; bytecode versioning notes |
| C | Command-Line Tools Cheat Sheet | All ~40 binaries from `clang` to `llvm-bolt` |
| D | Migration Notes: LLVM 18 → 22 | Opaque pointers; RemoveDIs; pass manager API churn; GlobalISel API changes; MLIR API churn; deprecated and removed APIs |
| E | Glossary | ~250 terms |
| F | Object File Format Reference | ELF, Mach-O, COFF/PE, WebAssembly |
| G | DWARF Debug Info Reference | DWARF 5 sections; abbreviation tables; tag and attribute reference; CFI; `.debug_names`; split DWARF |
| H | The C++ ABI: Itanium and Microsoft | Cross-reference for Ch 42 and 43 |

---

## Estimated Page Distribution

| Part | Title | Chapters | Pages |
|------|-------|----------|-------|
| I | Foundations | 5 | 85 |
| II | **Compiler Theory and Foundations** | 6 | 120 |
| III | **Type Theory** | 4 | 80 |
| IV | LLVM IR | 12 | 225 |
| V | Clang Internals: Frontend Pipeline | 11 | 210 |
| VI | Clang Internals: Codegen and ABI | 6 | 120 |
| VII | Clang as a Multi-Language Compiler | 7 | 140 |
| VIII | ClangIR (CIR) | 3 | 50 |
| IX | Frontend Authoring (Building Your Own) | 4 | 80 |
| X | Analysis and the Middle-End | 11 | 210 |
| XI | **Polyhedral Theory** | 4 | 80 |
| XII | Polly | 3 | 50 |
| XIII | Link-Time and Whole-Program | 4 | 80 |
| XIV | The Backend | 14 | 280 |
| XV | Targets | 13 | 250 |
| XVI | JIT, Sanitizers, and Diagnostic Tools | 11 | 210 |
| XVII | Runtime Libraries | 6 | 110 |
| XVIII | Flang | 4 | 80 |
| XIX | MLIR Foundations | 8 | 150 |
| XX | In-Tree Dialects | 10 | 190 |
| XXI | MLIR Transformations | 6 | 110 |
| XXII | XLA and the OpenXLA Stack | 6 | 120 |
| XXIII | MLIR in Production | 8 | 160 |
| XXIV | **Verified Compilation** | 5 | 110 |
| XXV | Operations, Bindings, Contribution | 5 | 100 |
| XXVI | Ecosystem and Frontiers | 7 | 108 |
| XXVII | **Mathematical Foundations and Verified Systems** | 6 | 120 |
| XXVIII | Language Ecosystems, Emerging Targets, and Engineering Practice | 7 | 86 |
| XXIX | Compiler Tooling, Kernel Integration, and Binary Analysis | 6 | 72 |
| XXX | **AI-First Programming Language Design** | 5 | 60 |
| | Appendices A–H | 8 | 95 |
| | **Total** | **207 chapters + 8 appendices** | **~2642 pages** |

---

## Suggested Volume Splits

A 2200-page single volume is a doorstop. Natural fascicle splits:

**Four-volume split (recommended at this size):**
- **Vol 1 — Foundations and Theory** (Parts I–III, IX, XI): ~445 pages — the conceptual base. Ships as "Compiler Theory, Type Theory, and the Polyhedral Model" — readable on its own without LLVM as a prerequisite.
- **Vol 2 — LLVM and Clang** (Parts IV–X, XII–XVIII): ~1665 pages... too big. Subsplit:
  - **Vol 2a — LLVM IR, Optimization, and Codegen** (IV, X, XII–XVI): ~1115 pages
  - **Vol 2b — Clang and Runtimes** (V–VIII, XVII–XVIII): ~870 pages
- **Vol 3 — MLIR and ML Compilation** (Parts XIX–XXIII): ~730 pages — the ML compiler stack
- **Vol 4 — Verified Compilation and Engineering Practice** (XXIV–XXVI + Appendices): ~399 pages

So really **six volumes** at this size:
- Vol 1: Theory (445 pp) — Parts I–III, IX, XI
- Vol 2: LLVM Core (1115 pp) — Parts IV, X, XII–XVI
- Vol 3: Clang and Runtimes (870 pp) — Parts V–VIII, XVII–XVIII
- Vol 4: MLIR and ML Compilation (730 pp) — Parts XIX–XXIII
- Vol 5: Verified Compilation + Operations + Ecosystem (413 pp) — Parts XXIV–XXVI
- Vol 6: Mathematical Foundations, Ecosystems, AI-First PLs, and Appendices (433 pp) — Parts XXVII–XXX + A–H

Total across volumes: ~3934 pages of *target* material — consolidated single set is ~2642 pages.

 
## Writing Guidance
- use a team of agents and /loop - keep context threshold in mind on planning how many chapters to do at once. 
- keep a TODO_checklist.md doc of what is done and what is next, so that continuation after compaction is smooth
- plan out each chapter before writing
- add reference links inline as needed
- review for accuracy of code after each chapter is done. then git commit.
- before compaction approaching compaction threshold, suggest improvements to content and anything that may be missing. keep a running list of suggested improvements in the TODO_checklist.md
---

---

---

*Outline pinned to LLVM 22.1.x and corresponding MLIR / OpenXLA snapshot as of April 2026. Theoretical chapters anchored to the canonical literature cited at the head. ClangIR, CUDA Tile IR, Triton, Mojo, ML Inliner/Regalloc, and the Verified Compilation chapters track moving research and engineering frontiers. Part XXVI (Ecosystem and Frontiers, 7 chapters) added April 2026: rustc internals (MIR/Miri/Polonius/a-mir-formality/Cranelift), Rust compiler ecosystem (inkwell/llvm-sys/melior/gimli/Burn/CubeCL), LLVM/MLIR for AI, AI-guided compilation, formal verification in practice (Dafny/Verus/F*/HACL*/Kani/Flux/LLM provers), language tooling (Logos/Pest/LALRPOP/Rowan/Chumsky/Winnow/ANTLR4/TreeSitter), and modern C++ for compiler development (C++23 std::expected/mdspan/deducing-this, C++26 Contracts/P2900, C++26 Static Reflection/P2996, pattern matching). Part XXVII (Mathematical Foundations and Verified Systems, 6 theoretical chapters) added April 2026: proof assistant internals (Lean 4 LCNF pipeline/Coq extraction/Isabelle LCF), mathematical logic and model theory (FOL completeness/incompleteness/decidable fragments/DPLL(T)/SMT), verified hardware (CHERI LLVM with address-space-200 capabilities/Morello/CherIoT/seL4 three-level refinement/Sail ISA spec), commutative algebra for compilation (Gröbner bases/toric varieties/Ehrhart theory/algebraic path problem/tropical semiring), category theory for compiler engineers (functors/adjunctions/monads/Curry-Howard-Lambek/2-categories), denotational semantics and domain theory (Scott domains/PCF/game semantics/process calculi CCS/π-calculus/CSP). Moves four previously out-of-scope areas in-scope. Part XXVIII (Language Ecosystems, Emerging Targets, and Engineering Practice, 7 chapters) added April 2026: CIRCT hardware compiler (FIRRTL/HW/Comb/Seq/Handshake/Arc dialects, firtool, HLS), quantum compilation (QIR/QUIR/MLIR quantum dialects/ZX-calculus/error correction), Swift SIL (@owned/@guaranteed ownership model/ARC optimizer/influence on MLIR bufferization), Julia (type-inference-driven LLVM specialization/multiple dispatch/GPU compilation), Zig (ZIR→AIR→LLVM IR/comptime metaprogramming/C cross-compiler), safety-critical toolchain qualification (DO-178C/ISO 26262/IEC 61508/Ferrocene/MISRA/SPARK), cross-language ABI interoperability (bindgen/cbindgen/cxx/UniFFI/WIT components). Part XXIX (Compiler Tooling, Kernel Integration, and Binary Analysis, 6 chapters) added April 2026: Clang plugin system (PluginASTAction/CodeGenPlugin/PragmaHandler), value tracking infrastructure (KnownBits/ConstantRange/DemandedBits/LazyValueInfo), llvm-mca static performance analysis (pipeline simulation/scheduling model validation), Linux kernel compilation with LLVM/Clang (ClangBuiltLinux/kCFI/ThinLTO/BPF+BTF/KASAN/objtool/GKI), binary lifting to LLVM IR (Remill/McSema/RetDec/CFG recovery), Apache TVM (Relay/TIR/TVMScript/AutoTVM/Ansor/BYOC/microTVM). Additionally, seven existing chapters expanded: Ch79 (linker relaxation), Ch80 (reproducible builds/supply-chain security), Ch106 (WASI/WasmGC), Ch110 (DFSan), Ch133 (MLIR external models), Ch140 (MLIR Presburger library), Ch173 (mlir-lsp-server). Part XXX (AI-First Programming Language Design, 5 chapters) added May 2026: migrated from adjacent research project; covers AI-first PL principles and landscape (MoonBit/Pel/Dana/Dafny/Lean/Verus/Unison/Koka/LMQL/DSPy/PPL bridge — Stan/Pyro/Gen), formal language specification (6-layer EBNF through metatheory), transformer model development PLs (Mojo/Dex/Futhark/Exo 2/Burn with MLIR backends), multi-agent PLs and AI-first SDLC (Choral/HasChor/Pijul/Nickel/Dagger + Pony/E/Jif/Flow Caml security + 9-row paradigm failures table), and reflective/self-improving code (Lean 4 MetaM/AlphaProof/Maude/rho-calculus + G1–G7 open problems + Phase 0–5 build roadmap). Total: 207 chapters + 8 appendices, ~2642 pages.*


---

@copyright jreuben11
